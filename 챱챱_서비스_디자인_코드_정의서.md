# 챱챱 서비스·디자인·코드 정의서

> 버전: 2026-07-29  
> 목적: 구독 전용 챱챱 서비스의 범위, UX/UI, 색상, 컴포넌트, Vue 3 구현 규칙을 한 기준으로 관리한다.

## 1. 서비스 정의와 범위

챱챱은 단건 상품을 판매하는 마켓이 아니라, 사용자가 플랜을 선택하고 배송·메뉴 구성을 관리하는 **정기 구독 서비스**다.

### 확정 범위

- 메뉴는 독립 판매 상품이 아닌 **플랜에 포함되는 선택 항목**이다.
- 메뉴별 개별 가격은 표시하지 않는다.
- 플랜은 고정 가격 구조지만, 실제 금액이 정해질 때까지 고객 화면에는 **가격 미정**으로 표시한다.
- 단건 마켓, 장바구니, 단건 주문, 단건 주문 이력은 제공하지 않는다.
- 결제·배송·환불 이력은 모두 구독 및 배송 회차와 연결한다.

### 핵심 흐름

```text
플랜 선택
→ 배송 요일·시간 설정
→ 배송지 선택
→ 주차별 메뉴·수량 구성
→ 신청 내용 확인
→ 결제수단·자동결제 동의
→ 구독 생성 및 회차 관리
```

## 2. 화면 범위

전체 화면은 `WF-001~055`로 관리한다.

| 영역 | 화면 | 목적 |
|---|---:|---|
| 공개·인증 | 001~007 | 시작, 로그인, 회원가입, 비밀번호, 소셜 연동 |
| 메뉴 | 008~009 | 플랜 포함 메뉴 탐색 및 상세 |
| 플랜 | 010~012 | 체험·Solo·Family 비교 및 상세 |
| 구독 신청 | 013~019 | 배송부터 결제까지의 단계형 신청 |
| 내 구독 | 020~025, 054 | 구독·회차 관리 및 플랜 변경 |
| 마이페이지 | 026~036 | 회원·배송지·카드·결제·배송·환불 이력 |
| 관리자 | 037~053, 055 | 메뉴·재고·배송·구독·결제 운영 및 대시보드 |

- `WF-054`: 플랜 변경하기
- `WF-055`: 관리자 대시보드
- 이전 마켓 화면 `WF-054~060`은 구독 전용 범위에서 제거한다. 번호 재사용으로 인한 혼동을 막기 위해, 문서와 코드에서는 새 `WF-054`만 플랜 변경 화면으로 취급한다.

## 3. 구독 정책과 표시 원칙

| 항목 | 확정 정책 | 화면 반영 |
|---|---|---|
| 플랜 가격 | 고정 가격, 현재 미정 | 플랜 가격은 `가격 미정`; 메뉴별 가격 미표기 |
| 가격·배송비 | 백엔드가 최종 계산·검증 | 프론트는 API가 준 플랜가·배송비·총액만 표시 |
| 구독 해지 | 다음 정기결제부터 제외 | 현재 구독 기간과 이미 생성된 배송 회차는 유지 |
| 플랜 변경 | 다음 결제 기간부터 적용 | 내 구독 상세의 `플랜 변경하기`에서 별도 페이지로 이동 |
| 체험 종료 | Solo 또는 Family로 전환 유도 | 모바일 Bottom Sheet, PC 다이얼로그. 백엔드의 체험 종료 상태일 때만 노출 |
| 메뉴 제한 | 재고·수량·플랜 기준 제한 | 불가 메뉴 비활성화, 수량 제한 상태와 사유를 함께 표시 |
| 환불 | 상담 채팅 중심으로 변경 가능 | 상세 정책·폼은 확정 전까지 고정 구현하지 않음 |

### 가격 API 원칙

프론트엔드는 최종 금액을 계산하거나 확정하지 않는다. 금액이 확정되기 전에는 `null` 또는 상태값을 받아 `가격 미정`으로 안내하고, 확정 후에도 서버가 내려준 견적을 표시한다.

```js
{
  planId: 'solo',
  planPrice: null,
  deliveryFee: null,
  totalAmount: null,
  pricingStatus: 'PENDING'
}
```

## 4. 정보 설계와 가독성

각 페이지는 아래 흐름을 기본으로 한다.

```text
페이지 목적
→ 현재 상태 또는 핵심 정보
→ 선택·입력 영역
→ 다음 행동
```

- 페이지마다 H1은 하나만 둔다.
- 제목 아래에는 목적을 설명하는 짧은 문장만 둔다.
- 한 화면의 주요 CTA는 하나로 제한한다.
- 카드 하나에는 목적 하나만 담는다.
- 긴 문단보다 `라벨 + 값` 형식으로 정보를 보여 준다.
- 섹션 간 여백은 넉넉히, 카드 내부 요소 간격은 촘촘히 둔다.
- 구독 서비스에서는 메뉴 설명보다 `현재 구독`, `다음 배송`, `변경 마감`, `다음 결제/자동결제 상태`를 우선 배치한다.
- 색상만으로 상태를 전달하지 않는다. 상태명, 아이콘, 테두리 또는 배경을 함께 사용한다.

## 5. 반응형과 행동 배치

- 고객 화면은 **Mobile First**, 관리자 화면은 **PC First**로 설계한다.
- 모바일 고객 화면의 상단 다중 내비게이션은 하단 고정 내비게이션으로 옮긴다: `홈 · 메뉴 · 플랜 · 내 구독 · 마이`.
- 주요 버튼은 화면 하단 또는 오른손 엄지 접근이 쉬운 위치에 둔다.
- 구독 신청 단계에는 하단 고정 CTA를 사용하고, 본문이 버튼에 가려지지 않도록 하단 안전 여백을 둔다.
- PC에서는 넓은 화면에 맞춰 콘텐츠 폭을 제한하고, 카드·대시보드·표를 그리드로 구성한다.
- 작은 화면의 관리자 페이지는 정보를 숨기기보다, 중요 KPI와 필터를 우선하고 표는 가로 스크롤 또는 요약 카드로 전환한다.

## 6. 모달, Bottom Sheet, Toast

### 모달 또는 확인창을 쓰는 경우

- 구독 해지 최종 확인
- 자동결제 동의
- 배송지 삭제
- 결제 재시도
- 체험 종료 후 플랜 전환 제안

### 모달을 쓰지 않는 경우

- 메뉴 상세
- 플랜 비교
- 배송 일정 선택
- 메뉴 수량 변경
- 배송·결제 이력 상세

모바일의 빠른 선택은 중앙 모달보다 Bottom Sheet를 우선한다. 단순 완료·실패 안내는 Toast, 긴 설명이나 세부 확인은 별도 페이지 또는 인라인 영역을 사용한다.

## 7. 색상 팔레트

| 역할 | 토큰 | 값 | 사용 |
|---|---|---|---|
| 주요 브랜드 | `--color-primary` | `#96A75E` | 일반 주요 CTA, 선택, 활성 상태 |
| Primary Hover | `--color-primary-hover` | `#A4B46E` | Hover |
| Primary Pressed | `--color-primary-pressed` | `#7D8E49` | Pressed |
| Primary Soft | `--color-primary-soft` | `#F5F7EC` | 선택 카드·활성 배경 |
| 보조 브랜드 | `--color-secondary` | `#F28C28` | 체험·추천·전환 |
| Secondary Soft | `--color-secondary-soft` | `#FFF3E5` | 체험·추천 카드 배경 |
| 전체 배경 | `--color-background` | `#FFFDF9` | 서비스 배경 |
| 표면 | `--color-surface` | `#FFFFFF` | 카드, 입력창, 표 |
| 보조 표면 | `--color-surface-subtle` | `#F8F8F4` | 보조 영역 |
| 기본 글자 | `--color-text` | `#33352F` | 제목, 본문, 버튼 글자 |
| 보조 글자 | `--color-text-muted` | `#7B7F75` | 설명, 날짜, 힌트 |
| 테두리 | `--color-border` | `#ECEFE5` | 카드, Input, Divider |
| 비활성 | `--color-disabled` | `#E1E4DC` | 비활성 버튼·메뉴 |
| 성공 | `--color-success` | `#5D8B70` | 완료, 정상 |
| Success Soft | `--color-success-soft` | `#EDF6F0` | 성공 안내 배경 |
| 경고 | `--color-warning` | `#C9982E` | 마감 임박, 수량 부족 |
| Warning Soft | `--color-warning-soft` | `#FFF8DC` | 경고 안내 배경 |
| 위험 | `--color-danger` | `#C96359` | 실패, 삭제, 오류 |
| Danger Soft | `--color-danger-soft` | `#FCEDEC` | 위험 안내 배경 |
| 정보 | `--color-info` | `#6689A3` | 처리 중·일반 안내 |
| Info Soft | `--color-info-soft` | `#EDF4F8` | 정보 안내 배경 |

### 색상 사용 규칙

- 일반 CTA는 올리브 Primary를 사용한다.
- 오렌지는 체험 시작·Solo 추천·체험 종료 전환처럼 한정된 전환 행동에만 사용한다.
- Warning은 오렌지와 구분한다. Warning은 주의·마감·부족 상태에만 사용한다.
- Solo 플랜은 추천 배지와 Soft 오렌지 배경으로 강조한다.
- 올리브·오렌지 CTA의 텍스트는 대비를 위해 기본적으로 `#33352F`를 사용한다.

## 8. 컴포넌트 설계

```text
디자인 토큰
→ 기본 UI 컴포넌트
→ 구독 업무 컴포넌트
→ 페이지 조합
```

### 기본 UI 컴포넌트

`BaseButton`, `BaseInput`, `Chip`, `StatusBadge`, `NoticeBox`, `EmptyState`, `Toast`, `BottomSheet`, `ConfirmDialog`

### 구독 업무 컴포넌트

`PlanCard`, `PlanSummary`, `SubscriptionStepper`, `MenuCard`, `QuantitySelector`, `DeliveryScheduleCard`, `SubscriptionHeader`, `CycleCard`, `HistoryListItem`, `PaymentMethodCard`

### 관리자 컴포넌트

`AdminLayout`, `AdminKpiCard`, `AdminFilterBar`, `AdminDataTable`, `AdminChartPanel`, `AdminStatusTag`

컴포넌트는 단순히 모양이 같아서가 아니라 **역할·상태·행동이 반복될 때** 만든다. 예를 들어 결제·배송·환불 상태 표시는 모두 `StatusBadge`의 정해진 상태 체계를 사용한다. 페이지 파일은 조합과 배치에 집중하고, 복잡한 계산·API 요청은 페이지에서 분리한다.

## 9. 관리자 대시보드

관리자 첫 화면에는 아래 정보를 배치한다. 모든 수치와 집계 기준은 백엔드에서 제공한다.

- 현재 구독자 수
- 기간 기준 수익
- 전체 회원 수
- 가입 경로 분포 원형 그래프: Google, 카카오, 챱챱 자체 회원, 기타
- 마감 임박·결제 실패·배송 준비 같은 운영 알림
- 기간별 구독자 또는 수익 막대 그래프

고객용 화면의 카드 스타일을 그대로 복제하지 않는다. 관리자는 빠른 비교와 밀도 있는 정보 확인이 목적이므로 표·필터·차트·상태 태그를 우선한다.

## 10. Vue 3 + JavaScript 코드 규칙

### 기술 범위

- Vue 3 + Vite
- JavaScript 사용. TypeScript는 도입하지 않는다.
- Vue Router로 페이지 이동과 권한 진입을 관리한다.
- Pinia로 로그인·사용자·구독처럼 공유되는 상태를 관리한다.
- Axios로 백엔드 API를 호출한다.

### 폴더 방향

```text
src/
├─ pages/        # 화면 조합
├─ components/   # 재사용 UI·업무 컴포넌트
├─ stores/       # Pinia 공유 상태
├─ routes/       # 라우터 정의
├─ api/          # Axios 요청 함수
└─ styles/       # 토큰·공통 스타일
```

### 현재 익숙한 문법

`<script setup>`, `defineProps`, `defineModel`, `v-model`, `v-if`, `v-else`, `v-for`, `:속성 바인딩`, `@이벤트`, `ref`, `reactive`, `async/await`, `try/catch`, Pinia Store, Vue Router, CSS Grid/Flex

### 새 문법 도입 규칙

`computed`, `watch`, `defineEmits`, `slot`, `Transition`, `Teleport`, composable은 필요한 때만 도입한다. 처음 사용하는 파일에서는 해당 코드 바로 위에 아래 세 가지를 짧은 주석으로 적는다.

1. 이 문법이 무엇인지
2. 어떤 기능을 하는지
3. 이 코드에서 어떻게 동작하는지

같은 문법의 설명 주석을 모든 파일에 반복하지 않는다. 페이지는 화면 조립에 집중하고, API 호출·공유 상태·복잡한 계산은 Store, API 함수 또는 별도 함수로 분리한다.

## 11. 라이브러리 기준

초기 라이브러리는 아래 **7개**로 고정한다. 별도 CSS 프레임워크, 모달·토스트 라이브러리, 차트용 Vue 래퍼는 추가하지 않는다.

| 라이브러리 | 역할 | 사용 위치 |
|---|---|---|
| `lucide-vue-next` | 아이콘 | 고객·관리자 공통 |
| `@formkit/auto-animate` | 목록·카드의 자연스러운 추가·삭제·정렬 전환 | 고객 화면 우선 |
| `primevue` | 표, 필터, 날짜 선택, 관리자 알림·확인 UI | 관리자 전용 |
| `pinia` | 로그인·사용자·구독 공유 상태 | 공통 기능 |
| `axios` | 백엔드 API 통신 | 공통 기능 |
| `chart.js` | 관리자 원형·막대 그래프 엔진 | 관리자 대시보드 |
| `dayjs` | 날짜 표시, 배송·결제일 계산 | 고객·관리자 공통 |

### PrimeVue 사용 경계

- PrimeVue는 `/admin` 경로와 `Admin*` 컴포넌트에서만 사용한다.
- 고객 화면은 직접 만든 `Base*` 및 구독 업무 컴포넌트로 구성한다.
- 관리자에서는 `DataTable`, `Paginator`, `DatePicker`, `Select`, `MultiSelect`, `Dialog`, `ConfirmDialog`, `Toast`, `Tag`, `Skeleton`, `Chart`를 우선 사용한다.
- 초기에는 PrimeVue 스타일 모드와 챱챱 팔레트 기반 테마를 사용한다. Unstyled 모드는 내부 스타일링 경험이 쌓인 뒤 검토한다.

### Day.js 사용 경계

- 날짜 표시, 다음 배송일, 변경 마감일, 결제일 등 **표시와 계산 보조**에 사용한다.
- 정기 배송 주기·가격·환불 가능 여부의 최종 판정은 백엔드가 담당한다.
- 시간대 기준값은 API가 내려준 값을 우선하며, 프론트에서 임의로 업무 날짜를 확정하지 않는다.

## 12. 구현 순서

1. 단건 마켓 경로·문구·내비게이션을 제거하고 구독 전용 흐름을 확정한다.
2. 색상·간격·타이포그래피 토큰을 적용한다.
3. 고객 공통 UI 컴포넌트를 만든다.
4. 플랜 화면을 개편하고 Solo 추천과 체험 CTA를 반영한다.
5. 구독 신청 화면을 고정 가격 구조와 `가격 미정` 상태에 맞춰 정리한다.
6. 내 구독·회차·마이페이지를 구독 상태 중심으로 개편하고 `플랜 변경하기`를 연결한다.
7. 모바일 Bottom Navigation·하단 고정 CTA·반응형을 검수한다.
8. 관리자 `AdminLayout`과 PrimeVue 기반 운영 화면을 구성한다.
9. 관리자 대시보드에 KPI·가입 경로 차트·운영 알림·기간별 그래프를 연결한다.
10. 실제 API 연동 후 금액·배송비·수량 제한·체험 종료·해지 상태를 서버 기준으로 검증한다.

## 13. 추후 확정이 필요한 항목

- 실제 플랜 가격과 배송비 정책
- 플랜 변경 마감 시점
- 메뉴별 재고·수량 제한의 정확한 백엔드 규칙
- 환불 상담 채팅의 진입 위치, 운영 시간, 처리 단계
- 체험 종료 감지 시점과 전환 제안 노출 빈도
- 관리자 수익·회원·구독자 집계 기간과 결제 취소·환불의 포함 기준

## 14. 작성 후 자체 검수 기록

아래 항목을 확인했다.

- 서비스 범위에서 단건 마켓을 제거했고, 화면 번호 정책과 충돌하지 않도록 새 `WF-054`의 의미를 명시했다.
- 가격은 고정 구조이면서 현재 `가격 미정`이라는 상태를 구분했고, 메뉴별 가격 미표기·백엔드 최종 계산 원칙과 일치시켰다.
- 해지·플랜 변경·체험 종료·메뉴 제한·환불의 정책과 화면 표현을 각각 연결했다.
- 고객 화면은 Mobile First·직접 제작 컴포넌트, 관리자 화면은 PC First·PrimeVue 전용이라는 경계를 일관되게 적용했다.
- 색상 규칙에서 Primary·체험 오렌지·Warning의 역할을 분리했다.
- 7개 라이브러리의 역할과 사용 영역을 분리했고, Chart.js와 Day.js가 PrimeVue와 중복되지 않음을 명시했다.
- 확정되지 않은 정책은 구현 확정 사항으로 쓰지 않고, 마지막 확정 필요 항목으로 분리했다.
