# 10. ADR

> 한국어 별칭: 아키텍처 결정 기록  
> 목적: 하네스의 중요한 구조적 결정을 기록한다.  
> 원칙: ADR은 작업 절차 문서가 아니라, 되돌리기 어려운 결정과 그 이유를 남기는 기록 문서다.

---

## ADR 작성 규칙

- ADR 번호는 문서 전체에서 고유해야 한다.
- 새 결정은 가장 마지막 ADR 번호 다음 번호를 사용한다.
- 기존 ADR 번호는 다른 결정을 위해 재사용하지 않는다.
- 번호를 수정한 경우 관련 Report와 Working Context의 참조도 함께 확인한다.
- 어떤 ADR의 결정이 이후 ADR로 대체되면, 상태를 `Accepted`에서 `Superseded by ADR-NNN`으로 바꾼다. 오래된 ADR을 지우지 않고 상태만 갱신한다(외부 검수 m-04).
- ADR의 여러 결정 중 **일부만** 후속 ADR이 바꾸면 전체를 `Superseded`로 표시하지 않는다. `Partially superseded by ADR-NNN (해당 부분만 한 줄 설명)`으로 표시해, 나머지 결정은 여전히 유효하다는 것을 명확히 한다(외부 검수 G-07).

---

## ADR-001. ECC 전체 복제 대신 중간 강도 반입을 선택한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | ECC 선택 참고자료를 전체 복제하지 않고, 일반 하네스에 맞는 개념과 구조만 재작성한다. |
| 이유 | 전체 Agent, Command Registry, Hook 구조는 현재 단계에서 무겁다. |
| 결과 | 핵심 문서 00~10, 스킬 8개, Gate 6개, Checklist 5개, 최소 scripts 3개 구조를 사용한다. |

---

## ADR-002. 자동화는 수동 실행형 검증부터 시작한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | 초기에는 Hook 자동 차단이 아니라 `scripts/*.mjs` 수동 실행 방식으로 검증한다. |
| 이유 | 자동 차단은 오작동 시 작업 흐름을 막을 수 있다. |
| 결과 | `validate-harness`, `validate-skills`, `validate-docs`만 우선 사용한다. |

---

## ADR-003. 결제와 보안은 일반 기능과 분리한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | 결제/보안 기능은 별도 Gate와 Red/Blue Checklist를 적용한다. |
| 이유 | 금전, 권한, 개인정보 리스크는 일반 CRUD보다 실패 비용이 크다. |
| 결과 | `payment-gate.md`, `security-gate.md`, `payment-red-blue-checklist.md`를 유지한다. |

---

## ADR-004. 하네스는 모든 스킬보다 상위 규칙이다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | `00.HARNESS_RULES.md`, `04.GATEGUARD.md`, `07.SECURITY_BASELINE.md`, `08.QUALITY_GATE.md`를 모든 스킬보다 상위로 둔다. |
| 이유 | 스킬 절차가 안전 규칙, 사용자 최신 요청, GateGuard 결과를 덮어쓰는 것을 막기 위함이다. |
| 결과 | 모든 `skills/*/SKILL.md`에 Harness Control Rule을 추가한다. |

---

## ADR-005. 위험 작업 WARN은 기본 중단/확인으로 처리한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | 파일 삭제, 대량 이동, DB, 결제, 인증/권한, 외부 스크립트 실행의 WARN은 기록 후 진행이 아니라 중단/확인으로 처리한다. |
| 이유 | 위험 작업은 일부 불명확한 상태에서도 사고로 이어질 수 있다. |
| 결과 | `04.GATEGUARD.md`, `08.QUALITY_GATE.md`, `09.AGENT_WORKFLOW.md`에 WARN 분기 규칙을 추가한다. |

---

## ADR-006. 자동 검증 PASS는 구조 PASS로만 해석한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | `scripts/*.mjs`의 PASS는 구조 검증 통과로만 해석한다. |
| 이유 | 필수 파일/섹션 존재가 문서 의미 품질, 보안 타당성, 결제 로직 안전성을 보장하지 않기 때문이다. |
| 결과 | `08.QUALITY_GATE.md`, scripts 출력, Report 기준에 PASS 한계를 명시한다. |


---

## ADR-007. v0.2.1 안정화 패치

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | v0.2 감사에서 상위 안전 규칙이 일부 하위 Gate/Script/Checklist에 동기화되지 않은 문제가 발견됨 |
| 결정 | 새 기능을 추가하지 않고 `Harness Control Rule` 필수 검증, 위험 WARN 분기, 사전/사후 Gate 구분, 전역 표현 중립화를 우선 반영함 |
| 결과 | 스킬 확장 안전성과 위험 산출물 검수 일관성이 강화됨 |


---

## ADR-008. v0.2.2 유지보수 패치

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | v0.2.1 감사에서 ADR 번호 중복, 최신 상태 표기 혼재, 과거 Report의 다음 작업 충돌 가능성, `Harness Control Rule` 본문 검사 완화가 발견됨 |
| 결정 | 새 스킬이나 대형 자동화를 추가하지 않고 ADR 번호 고유성, 최신 Report 우선 읽기, superseded 표시, HCR 최소 요건 강화, Handoff 대상 존재 검사를 반영함 |
| 결과 | 장기 유지보수와 상태 추적성이 개선되고, 새 스킬 확장 시 연결 오류를 더 빨리 발견할 수 있음 |


---

## ADR-009. 배포 패키지명은 안정 이름을 사용한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 버전 suffix가 붙은 ZIP 루트명과 검증 스크립트의 루트 탐색 규칙이 충돌하여 상위 폴더 실행이 실패할 수 있음 |
| 결정 | 배포 파일명은 `GENERAL_HARNESS.zip`, 압축 해제 루트는 `GENERAL_HARNESS/`로 고정하고, 버전 이력은 내부 문서에만 기록함 |
| 결과 | 스크립트 실행 위치가 단순해지고, 사용자와 Agent가 동일한 루트명을 기준으로 작업할 수 있음 |

---

## ADR-010. Handoff 스킬 표기는 canonical 경로로 고정한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | Skill Gate는 bare skill name을 허용했지만 `validate-skills.mjs`는 경로 중심으로 검사해 문서와 자동 검증이 어긋날 수 있었음 |
| 결정 | Handoff 대상 스킬은 `skills/{skill-name}/SKILL.md` 형식으로만 표기함 |
| 결과 | 스킬 Handoff 오타와 존재하지 않는 대상 연결을 더 안정적으로 검증할 수 있음 |


---

## ADR-011. 안정 패키지 정합성 검증을 추가한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 결정 | 배포 파일명/루트명은 `GENERAL_HARNESS.zip` / `GENERAL_HARNESS/`로 고정하고, `validate-package.mjs`로 기본 패키지명 정책을 검증한다. |
| 이유 | 버전 suffix가 붙은 루트명은 스크립트 실행 위치와 문서 기준을 흔들 수 있다. |
| 결과 | 버전 이력은 내부 문서에 남기고, 패키지 이름은 안정 이름을 유지한다. |


---

## ADR-012. Runtime Slim Patch

| 항목 | 내용 |
|---|---|
| 상태 | Accepted(단, "05.WORKING_CONTEXT.md 분리는 보류한다" 부분은 `Superseded by ADR-027` — 이후 실제로 신규 프로젝트 템플릿으로 전환됨) |
| 결정 | 일반 S/M 작업에서는 `00.HARNESS_RULES.md` 전체 대신 `00.QUICK_REF.md`를 우선 로딩한다. 각 스킬의 Harness Control Rule은 2줄 요약으로 축약하고, 상세 규칙은 `01.SKILL_TEMPLATE.md`, `00.QUICK_REF.md`, `00.HARNESS_RULES.md`에 둔다. |
| 이유 | 매 작업마다 전체 규칙과 반복 문구를 읽으면 토큰 비용이 커진다. 런타임에 필요한 충돌 우선순위, 위험 작업 기준, PASS/WARN/FAIL, 스킬 상하관계만 빠르게 확인하도록 분리한다. |
| 결과 | 하네스 안전 원칙은 유지하면서 일반 작업의 기본 로딩량을 줄인다. (당시 기준) `05.WORKING_CONTEXT.md` 분리는 보류했으나, 이후 ADR-027에서 신규 프로젝트 템플릿으로 전환하며 사실상 대체됨(외부 검수 m-06). |

---

## ADR-013. 외부 반입 후보는 재작성해서 필요한 부분만 반입한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `harness_import_candidates_20260704`(ECC, Understand-Anything, codebase-memory-mcp에서 선별한 후보 자료)가 제공됨. 원본은 npm/TypeScript 전제, Hook 자동화, 실행자 분기(Executor Boot와 별도 에이전트 문서) 등 현재 하네스 전제와 다른 부분을 포함함 |
| 결정 | `verification-loop`, `agent-recovery` 스킬과 API 계약 상태(Candidate/Confirmed), Handoff Note, 스킬 대분류(문서/코드/운영) 개념만 하네스 문체로 재작성해 반입한다. Executor Boot 구조, Understand-Anything, codebase-memory-mcp, Hook 기반 자동화는 이번에 반입하지 않는다 |
| 이유 | `00.HARNESS_RULES.md §2-2`의 원본 직접 복제 금지 원칙과, 특정 스택에 종속되지 않는다는 일반 하네스 원칙을 지키기 위함. 실행자 분기 구조는 SRS 전용 용어(Source of Truth, Stop Condition 등)를 전제해 현재 문서 세트의 용어 체계와 충돌할 수 있어 별도 검토가 필요함 |
| 결과 | 활성 스킬이 8개에서 10개로 늘었고, `02.SKILL_INDEX.md`에 스킬 대분류가 추가됨. 반입하지 않은 항목은 `_PENDING_IMPORT_LIST.md`에 사유와 함께 보존함 |


---

## ADR-014. 구조 WARN을 정식 WARN 분류로 유지한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `DOCUMENT_AUDIT_REPORT.md`에서 `08.QUALITY_GATE.md`의 WARN 3분류와 하위 문서의 2분류 처리 기준이 충돌한다고 지적됨 |
| 결정 | `구조 WARN`을 삭제하지 않고 `일반 WARN` / `구조 WARN` / `위험 WARN` 3분류로 유지한다. 구조 WARN은 문서 연결, 스킬 인계, 번호 체계, 폴더 구조, 하네스 연결 구조가 모호한 경우에 사용하며, 일반 WARN처럼 바로 진행하지 않는다. |
| 이유 | 하네스 자체 수정과 문서 연결 변경은 파일 삭제나 DB 변경만큼 즉시 위험하지는 않지만, Source of Truth와 Handoff 흐름을 흔들 수 있으므로 별도 분류가 필요하다. |
| 결과 | `00.QUICK_REF.md`, `04.GATEGUARD.md`, `08.QUALITY_GATE.md`, 5개 Checklist에 구조 WARN 처리 기준을 동기화했다. |

---

## ADR-015. 혼합 운영 분기는 기존 Workflow와 Context Budget 안에서 처리한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 혼합 운영(Claude+Codex) 관련 외부 검수 피드백에서 Claude + Codex 혼합 운영 전제와 실행자별 First Reading 경로가 충분히 고정되어 있지 않다고 지적됨. 피드백 원본 문서는 현재 보존되어 있지 않으며 반영 결과만 근거로 남긴다 |
| 결정 | `00.EXECUTOR_BOOT.md`, `CLAUDE.md`, 단수형 에이전트 문서 같은 별도 부트 문서를 새로 만들지 않고, `03.CONTEXT_BUDGET.md §2-3`과 `09.AGENT_WORKFLOW.md §1-1`에 최소 역할 분기와 First Reading을 둔다. |
| 이유 | 일반 하네스의 문서 수를 불필요하게 늘리지 않고, Claude형 문서 판단 Agent와 Codex형 로컬 실행 Agent의 책임 경계만 명확히 하기 위함이다. |
| 결과 | 별도 실행자 분기 구조는 `_PENDING_IMPORT_LIST.md`에 장기 보류로 유지하되, 현재 혼합 운영에 필요한 최소 규칙은 기존 문서 안에서 사용할 수 있다. |

---

## ADR-016. 하네스 이름을 GLOBAL_AI_HARNESS에서 GENERAL_HARNESS로 바꾼다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 사용자가 "글로벌 하네스"라는 이름 대신 담백한 "일반 하네스"로 바꾸길 원함 |
| 결정 | 폴더명/zip명/패키지명은 `GENERAL_HARNESS`(영문)로, 문서 내 한국어 표현은 "일반 하네스"로 통일한다. 한글 폴더명은 터미널/CLI 도구(특히 Codex 같은 로컬 실행 Agent)에서 경로 처리가 도구마다 다르게 깨질 수 있어 사용하지 않는다. |
| 이유 | 이름을 화려하게 짓지 않는다는 방향은 유지하되, 실제 파일시스템에 닿는 이름은 영문으로 두어 혼합 운영(Claude+Codex)에서의 경로 안전성을 확보하기 위함이다. |
| 결과 | `GLOBAL_AI_HARNESS` 문자열과 "전역 하네스" 표현을 활성 문서 22개에서 일괄 치환했다. `reports/` 안의 과거(리네임 이전) Report는 당시 이름을 그대로 보존해 증거로 남긴다. |

---

## ADR-017. 외부 참고자료(Understand-Anything, codebase-memory-mcp)에서 개념만 재작성 반입한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `Understand-Anything`, `codebase-memory-mcp` 원본 저장소를 검토한 결과, 둘 다 설치형 완제품(모노레포/네이티브 바이너리, `curl \| bash` 설치)이라 직접 반입 대상은 아니지만, 각각에 재사용 가치가 있는 패턴이 있었다. |
| 결정 | Understand-Anything의 `docs/superpowers/specs`·`plans`에서 반복 확인된 "Goal/Non-Goals/Task 분해" 계획 패턴을 재작성해 `skills/planning/SKILL.md`로 신설한다. codebase-memory-mcp의 `check-no-test-skips.sh` 원칙("전제 조건 미비로 인한 SKIP은 FAIL이다")을 `skills/verification-loop/SKILL.md`에 반영한다. ECC 참고자료의 `validate-no-personal-paths.js` 아이디어를 `scripts/validate-no-personal-paths.mjs`로 재작성 반입한다. 두 저장소의 실제 코드/바이너리/설치 스크립트는 반입하지 않는다. |
| 이유 | `00.HARNESS_RULES.md §2-2`의 원본 직접 복제 금지 원칙, 외부 설치 스크립트 실행을 기본값으로 두지 않는다는 원칙을 지키면서도, 지난 검수에서 확인된 공백("코드 작성 전 계획 없음", "SKIP을 FAIL로 처리하지 않음", "개인 경로 노출 규칙이 문서에만 있고 자동 검사가 없음")을 메우기 위함이다. |
| 결과 | 활성 스킬이 10개에서 11개로 늘었고, 자동 검증 스크립트가 4개에서 5개로 늘었다. 두 저장소의 코드베이스 인덱싱/그래프 기능(Git diff 영향도 매핑 등)은 `_PENDING_IMPORT_LIST.md`에 계속 보류로 남긴다. |

---

## ADR-018. 외부 피드백(GENERAL_HARNESS_REPAIR_AND_SKILL_ROADMAP.md)의 최신성 동기화 진단을 전부 수용한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 외부 검수 피드백 문서가 `05.WORKING_CONTEXT.md`의 낡은 Report 경로 고정 지목, `03.CONTEXT_BUDGET.md`의 표 구조 깨짐, top-level 과거 Report의 superseded 미표시를 지적함. 직접 파일 대조 결과 전부 사실로 확인됨(검수 리포트 참고) |
| 결정 | (1) `05.WORKING_CONTEXT.md`의 현재형 고정 경로 문장을 "당시 기준"으로 재작성하고 `_LATEST.md` 참조 원칙을 상단에 명시. (2) `03.CONTEXT_BUDGET.md §4-1`의 깨진 표 행을 §4 표로 복구하고, §4-2 최신성 충돌 Stop Condition을 신설. (3) top-level에 남아있던 과거 Report 4개에 Superseded note를 추가하고 `reports/archive/`로 이동. (4) `skills/report-consistency/SKILL.md`, `scripts/validate-report-consistency.mjs`, `scripts/validate-references.mjs`를 신설. (5) `reference-integrity`는 스킬이 아니라 스크립트로만 구현. (6) `project-bootstrap`/`harness-maintenance`/`document-audit`/`validate-zip-internals.mjs`/`validate-adr-unique.mjs`는 피드백 문서 자신의 판단과 동일하게 보류 |
| 이유 | 하네스가 스스로 "최신 상태를 정확히 말하는 것"을 최우선 원칙으로 삼고 있는데(`00.HARNESS_RULES.md §4-1` Memory 승격 경로, `03.CONTEXT_BUDGET.md §4-1` 중복 읽기 방지 원칙과 같은 맥락), 정작 자기 자신의 최신 상태 서술이 여러 곳에서 어긋나 있었던 것은 이 원칙과 직접 모순되기 때문이다 |
| 결과 | 활성 스킬이 11개에서 12개로 늘었고, 자동 검증 스크립트가 5개에서 7개로 늘었다. top-level `reports/`에는 최신 Report 1개만 남는다. |

---

## ADR-019. 반복 확인된 실수 패턴 5건을 04.GATEGUARD.md §11 금지 패턴으로 승격한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `reports/`(및 `reports/archive/`) 전체를 훑어 "발견된 문제", "실수", "오탐" 관련 기록을 분류한 결과, 2회 이상 반복된 패턴 5건을 확인함: (1) 표 삽입 후 구조 확인 누락(3회), (2) 신규 검증 스크립트 시험 실행 없이 완성 처리(2회), (3) 상태 문서에 특정 시점 값을 "현재 기준"으로 고정 서술(반복), (4) Report 교체 시 superseded 표시 지연(4회 이상), (5) 같은 개념의 새 섹션 추가 시 관계 설명 생략(2회) |
| 결정 | `00.HARNESS_RULES.md §4-1` Memory 승격 경로("같은 유형이 2회 이상 반복되면 승격 검토")에 따라, 이 5건을 `04.GATEGUARD.md §11 금지 패턴` 표에 근거(반복 횟수, 발생 위치)와 함께 추가한다. 별도 Memory 파일은 만들지 않는다(현재 일반 하네스는 Memory 필수 문서를 두지 않기로 이미 결정함, `00.HARNESS_RULES.md §4`) |
| 이유 | 이 5건은 전부 "위험 작업"(구조/DB/보안/결제)은 아니지만, 하네스 자체 수정이라는 반복 작업 맥락에서 실제로 여러 번 재발했고 사용자가 직접 승격을 요청함. GateGuard §11은 이미 이런 유형(패턴-문제 쌍)의 표를 갖고 있어 새 문서 추가 없이 바로 확장 가능함 |
| 결과 | 앞으로 하네스 자체를 수정하는 모든 작업은 `04.GATEGUARD.md §11`을 통해 이 5개 패턴을 사전에 점검하게 된다. 신규 발견 시 같은 방식(2회 반복 확인 후 승격)을 유지한다. |

---

## ADR-020. "자기 참조 색인/요약 갱신 누락" 패턴을 일반화해 승격한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `reports/archive/HISTORY_DIGEST.md`가 archive 폴더 실제 내용보다 뒤처진 문제를 발견해 수정하고 자동 검사를 추가했는데, 바로 다음 라운드에서 `05.WORKING_CONTEXT.md`의 "최신 1개 라운드만 남긴다"는 규칙(§7, ADR-019 이후 도입)이 3라운드째 지켜지지 않은 것을 발견함. 서로 다른 두 자기 참조 문서(archive 색인, Working Context 최신 요약)에서 같은 유형의 문제가 재발함 |
| 결정 | "새 라운드를 끝내면서 자기 참조용 색인·요약을 그 자리에서 갱신하지 않고 미룬다"는 패턴을 `04.GATEGUARD.md §11-3`에 일반화된 형태로 추가한다. 개별 사례(archive 색인, Working Context)로 따로 등록하지 않고 하나의 일반 패턴으로 합쳐 등록한다 |
| 이유 | 두 사례 모두 "라운드가 끝났는데 그 라운드의 흔적을 자기 참조 문서에 즉시 남기지 않았다"는 같은 원인에서 나왔다. 개별 사례별로 등록하면 세 번째 변형(예: 다른 색인 문서)이 나왔을 때 또 새로 등록해야 하므로, 원인 수준에서 한 번에 다룬다 |
| 결과 | `05.WORKING_CONTEXT.md`의 최신 3라운드(compression-cleanup-v1, history-wording-fix, history-digest-autocheck-v1)를 하나의 상태 요약으로 통합해 반영했다. 완전 자동 검사는 만들지 않았다 — "라운드가 반영됐는지"는 텍스트 대조만으로 판단하기 어려워, `06.REPORT_TEMPLATE.md`의 "Working Context 반영 여부" 항목을 사람이 매 라운드 확인하는 최소 안전장치로 유지한다. |

---

## ADR-021. 외부 검수 리포트(2026-07-10)의 Critical 5건 + Major 3건을 검증 후 수용한다

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `GENERAL_HARNESS_논리구조_검수_리포트_2026-07-10.md`가 Critical 5건, Major 16건, Minor 9건을 지적함. 5개 Critical 전부와 M-05를 직접 파일 대조로 검증한 결과 실제로 존재하는 문제였다(단, C-02와 X-06/M-06은 피드백의 프레이밍이 다소 과장돼 있었음 — 각각 "직접 충돌"이 아니라 "정의 누락", "역사 기록과 현재값 혼동"에 가까웠음) |
| 결정 | 55개 항목을 위험도·연관 파일 기준으로 6라운드로 나누기로 사용자와 합의함. 1라운드(C-01, C-02)와 2라운드(C-03, C-04, C-05, M-01, M-02, M-16)를 이번에 진행함: (1) FAIL 해제는 "수정 후 재검증 PASS"로만 가능하도록 `08.QUALITY_GATE.md §2`, `00.QUICK_REF.md §3`을 정정. (2) `04.GATEGUARD.md §6-1`을 신설해 위험 유형별 해제 조건(자동진행/사용자승인/유효한 근거/재검증)을 단일 SoT로 통합. (3) API Gate/Skill/Checklist 3곳의 PASS 판정에 계약 상태 표시를 실제로 반영. (4) 결제 웹훅 "부재"(FAIL)와 "부족"(WARN)의 경계를 명확히 구분. (5) 릴리즈 체크리스트에 테스트 증거·롤백·백업복구·배포후검증 항목 추가(N/A 허용). (6) 보안 Baseline/Gate의 PASS 정의를 "검토됨"에서 "필수 통제 충족"으로 강화하고 외부 응답 검증을 반영. (7) DB Gate/체크리스트에 필수값(NOT NULL)·유일성을 PASS/FAIL 판정에 반영. (8) Skill Gate FAIL 기준을 `validate-skills.mjs`의 실제 필수 섹션(7개)과 동일하게 맞춤 |
| 이유 | "필수 항목 표"와 "실제 PASS/FAIL 판정 행"이 여러 Gate/Checklist/Skill에서 독립적으로 관리되어 있어, 필수라고 선언한 항목이 실제 판정에는 반영되지 않는 같은 유형의 결함이 반복됨. 이번 라운드는 그 결함을 개별 수정하되, "필수 항목 = 판정 행에 반드시 반영"이라는 원칙을 이후 Gate/Checklist 작성·수정 시에도 지키기로 함 |
| 결과 | 14개 파일(`08.QUALITY_GATE.md`, `00.QUICK_REF.md`, `04.GATEGUARD.md`, `gates/api-gate.md`, `skills/api-design/SKILL.md`, `checklists/api-checklist.md`, `gates/payment-gate.md`, `checklists/payment-red-blue-checklist.md`, `checklists/release-checklist.md`, `07.SECURITY_BASELINE.md`, `gates/security-gate.md`, `gates/db-gate.md`, `checklists/erd-checklist.md`, `gates/skill-gate.md`)을 수정함. 나머지 47개 항목(3~6라운드 예정분)은 `_PENDING_IMPORT_LIST.md`에 라운드 계획과 함께 남겨둔다. |

---

## ADR-022. 외부 검수 리포트(2026-07-10) Round 3, 4 완료

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | ADR-021 이후 3라운드(M-03~M-06, P1-1, P1-3 — Report 원자적 갱신)와 4라운드(M-07, M-08, m-04, m-05 — 스크립트 보증범위 정직화)를 진행함 |
| 결정 | (Round 3) `06.REPORT_TEMPLATE.md §8`의 superseded 템플릿을 특정 파일 경로 대신 `reports/_LATEST.md` 참조로 변경(M-04 근본 원인). `§8-2`(신설)에 Report 완료 트랜잭션 9단계를 정의하고 `09.AGENT_WORKFLOW.md §8`이 이를 참조하도록 정리(M-03/M-05/P1-1). `validate-references.mjs`가 `reports/`도 검사하되 archive 이동 폴백을 적용하도록 재작성(M-04). `validate-report-consistency.mjs`가 스킬 이름 집합까지 구조적으로 비교하고 archive 폴더가 있는데 digest가 없으면 FAIL하도록 강화(M-06/P1-3). — (Round 4) `validate-package.mjs`가 실제 ZIP 내부(단일 루트/경로탈출/대소문자 중복/필수파일)를 검사하도록 재작성(M-07). `validate-harness.mjs`의 필수 파일 목록에 스크립트 7개 전부 추가(M-07). `validate-no-personal-paths.mjs`에서 실제 사용자 이름이 예외 목록에 들어가 있던 것을 제거하고 Unicode·추가 경로 패턴을 지원하도록 확장(M-08). `08.QUALITY_GATE.md §12`의 빈 줄로 끊긴 표를 복구(m-04). `§10-1` 보증범위 표에 누락됐던 스크립트 3개를 추가(m-05) |
| 이유 | Round 3은 이번 대화에서 여러 번 재발한 "자기 참조 갱신 누락" 문제의 근본 원인(superseded 템플릿이 특정 경로를 하드코딩)을 찾아 고친 것이다. Round 4는 자동 검증 스크립트들이 실제 보증 범위보다 과장된 PASS 메시지를 내고 있었다는 지적을 받아들여, 검증 범위를 실제로 넓히거나(ZIP 내부, 개인경로 패턴) PASS 메시지를 정직하게 좁혔다 |
| 결과 | 검증 스크립트 7개 중 4개(`validate-package.mjs`, `validate-no-personal-paths.mjs`, `validate-references.mjs`, `validate-report-consistency.mjs`)의 실제 검사 로직이 강화됨. 과거 리포트의 하드코딩된 후속 경로는 소급 수정하지 않고 그대로 두되(역사 기록 보존), 알려진 예외 3건만 스크립트에 명시적으로 등록함. 남은 라운드는 5, 6이다. |

---

## ADR-023. 외부 검수 리포트(2026-07-10) Round 5, 6 완료 — 55개 항목 전체 마무리

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | ADR-022 이후 5라운드(M-09~M-13, M-15 — Skill 라우팅/Output Format/Workflow 정합성)와 6라운드(m-01~m-03, m-06~m-09, M-14 — Minor 전부 + Source Mapping provenance)를 진행함. 이로써 최초 55개 항목(Critical 5·Major 16·Minor 9·충돌/중복/누락 25) 전부를 처리함 |
| 결정 | (Round 5) `08.QUALITY_GATE.md §7` 전체 흐름도가 사전 Gate 없이 시작하던 모순을 고치고 사전 Gate 분기를 추가(M-09). `skills/quality-gate/SKILL.md`의 Checklist를 조건부로 명시하고 placeholder Handoff를 실제 6개 Gate 결정표로 교체(M-10). `api-design`/`error-handling`/`security-review`/`terminal-ops`/`git-workflow` 5개 스킬의 Output Format에 실제 증거 필드를 추가(M-11). `verification-loop`의 자기모순 Do Not Trigger 문구를 정리하고 `agent-recovery`의 PASS/WARN 경계를 "사용자 입력 대기 여부"로 명확화(M-12). 5개 Checklist 전체에 결과(PASS/FAIL/N/A)·근거 열을 추가하고 `08.QUALITY_GATE.md §7-1`에 집계 규칙을 신설(M-13). `09.AGENT_WORKFLOW.md §2` 표준 흐름도에서 Report/WC 갱신을 조건부로 명시(M-15). — (Round 6) ADR 수치 정정(m-01), `_WORKING_CONTEXT_HISTORY.md`의 잘못된 소유문서 인용과 자기지시어 정정(m-02, m-03), Memory 최소형식에 근거 Report 링크 추가(m-06), `01.SKILL_TEMPLATE.md` 예시가 그대로 복사되면 안 된다는 점을 명시(m-07), archive Report의 표 파이프 이스케이프(m-08), 일부 bare 파일명 참조에 전체 경로 추가(m-09), `_SOURCE_MAPPING.md §0`에 외부 근거 출처·보존 상태 표 신설(M-14) |
| 이유 | 5라운드는 스킬/워크플로우 수준의 판정 모순(발동 조건과 종료 조건이 서로 다른 곳에서 다르게 서술됨)을 다뤘고, 6라운드는 지금까지의 이관·정리 작업 중 생긴 자잘한 인용 오류를 마무리했다 |
| 결과 | 외부 검수 리포트 55개 항목 전부 검증 후 반영 완료. 검증 7종 최종 재실행 결과는 해당 Report의 §4(검증 결과)와 §6(최종 개수 확정)을 참고. 이 대화에서 반복된 핵심 교훈(자기 참조 갱신 누락, 필수-판정 불일치, 발동/종료 조건 모순)은 전부 `04.GATEGUARD.md §11`, `08.QUALITY_GATE.md`, `06.REPORT_TEMPLATE.md §8-2`에 구조적으로 반영되어 이후 라운드에서도 유지된다. |

---

## ADR-024. 두 번째 외부 검수(ZIP 해시 대조 포함) — C-01 Report 게시 순서 결함 수정

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 같은 이름의 두 번째 외부 검수 리포트가 ZIP SHA-256 해시 대조, Windows 환경에서의 실제 재현까지 포함해 새로 도착함. 가장 중요한 지적(C-01)은 `06.REPORT_TEMPLATE.md §8-2`(직전 라운드에 방금 만든 Report 완료 트랜잭션) 자체의 순서 결함이었다 — 검증(6단계)보다 게시(`_LATEST.md` 갱신, 2단계)가 먼저였다. M-04는 같은 섹션의 5단계가 WC 갱신 소유 문서를 `03.CONTEXT_BUDGET.md §7`로 잘못 인용한 것을 지적했다. M-03/X-05는 5~6라운드를 완료한 뒤에도 `_PENDING_IMPORT_LIST.md`와 `_SOURCE_MAPPING.md`가 계속 "대기"로 남아있던 것을 지적했다 |
| 결정 | `06.REPORT_TEMPLATE.md §8-2`를 "초안 작성 → 검증 → (PASS/WARN일 때만) 게시 → 게시 후 재검증 → (실패 시) 롤백" 순서로 재구성한다. WC 갱신 소유 문서 인용을 `05.WORKING_CONTEXT.md §8`로 정정한다. `_PENDING_IMPORT_LIST.md §8`을 1~6라운드 전부 "완료"로 갱신하고, `_SOURCE_MAPPING.md`에 5~6라운드 반영 섹션을 신설한다. "게시가 검증을 앞지르는" 패턴과 "자기 참조 갱신 누락"의 3번째 재발(Pending/Source Mapping)을 `04.GATEGUARD.md §11-3`에 반영한다 |
| 이유 | C-01은 직전 라운드에 "자기 참조 갱신 누락"을 막으려고 만든 바로 그 절차 안에 있던 결함이라는 점에서 뼈아프다 — 문제를 막으려던 장치 자체가 같은 계열의 새 문제(검증 없는 게시)를 만들었다. 이번엔 순서를 바꾸는 구조적 수정과 함께, 실패 시 되돌리는 롤백 경로까지 명시해 같은 유형이 또 재발할 여지를 줄였다 |
| 결과 | Report 게시는 이제 검증 통과 후에만 일어난다. 검증 실패 시 `_LATEST.md`는 갱신되지 않으므로 미검증 상태가 현재 기준이 되는 일이 없다. Pending/Source Mapping의 완료 상태 반영 누락(3번째 재발)도 이번에 수정했다. |

---

## ADR-025. 두 번째 외부 검수 — 남은 Major 10건, Minor 6건 완료

| 항목 | 내용 |
|---|---|
| 상태 | Accepted (단, M-11의 "ZIP 미검사 시 WARN" 부분은 `Partially superseded by ADR-028` — 이후 미검사/파싱실패는 WARN이 아니라 FAIL로 더 강화됨. 나머지 결정은 유효) |
| 배경 | ADR-024(C-01) 이후 같은 검수 리포트의 남은 Major(M-01, M-02, M-05~M-12)와 Minor(m-01~m-06) 전부를 한 라운드로 처리함 |
| 결정 | M-01: `08.QUALITY_GATE.md §6`에서 사전 Gate WARN/FAIL을 분리하고 FAIL은 재검증 PASS만 인정하도록 명시, 흐름도 mermaid에 PRE 노드의 WARN/FAIL 분기를 명시적으로 그림(m-06 흐름도 설명 오류도 함께 정정). M-02: `09.AGENT_WORKFLOW.md §6`, `skills/terminal-ops/SKILL.md`, `00.QUICK_REF.md §2`에서 삭제·외부 실행은 "항상 실시간 확인, 과거 근거 대체 불가"로 통일. M-05: `skills/security-review/SKILL.md`에 외부 응답 인증성/무결성/타임아웃/실패정책 확인 절차와 판정 기준을 Baseline/Gate와 동기화. M-06: `gates/api-gate.md` 판정표에 Method/URL 반영, Checklist에 필드 타입/필수·nullable/제약/오류코드 항목 추가. M-07: Payment Gate/Checklist에 환불 상태전이·멱등성·보상처리·지연웹훅 시나리오 추가. M-08: DB Gate/ERD Checklist에 수량, 상태 전이, Hard/Soft Delete 선택을 판정까지 연결. M-09: SRS Checklist에 수용기준·행위자·입출력·전제조건·예외·검증방법·비기능요구 항목 추가(N/A 허용). M-10: Release Checklist에 "적용 범위"(제출용/운영배포용) 필드와 운영배포 전용 §1-1(모니터링·로그·임계값·책임자·관찰기간) 신설. M-11: `validate-package.mjs`를 재작성해 ZIP 미검사 시 PASS 대신 WARN을 반환하도록 fail-open 제거, 추출본-ZIP 전체 파일 parity 비교 추가(3가지 시나리오로 직접 테스트). M-12: `validate-references.mjs`에 `파일.md §N` 섹션 참조 존재 검사 추가(회귀 테스트로 확인), 3개 제외 문서는 여전히 검사 안 됨을 정직하게 문서화. m-01: `skills/planning/SKILL.md`에서 구현 단계가 생략된 것처럼 읽히던 문구 2곳 수정. m-02: `gates/document-gate.md`에 집계 규칙 참조 추가. m-03: `skills/quality-gate/SKILL.md` Output Format에 적용 Gate/Checklist, 구조/의미 PASS 구분 필드 추가. m-04, m-05: ADR-021/ADR-022의 파일·스크립트 개수 오기(8→14, 3→4) 정정. m-06 나머지: ADR-023의 잘못된 Report 섹션 참조(§5→§4/§6)를 정정 |
| 이유 | 대부분 "Gate/Checklist의 필수 항목 선언과 실제 판정표가 1:1이 아님"이라는 이미 등록된 D-017 원칙의 재발 사례였다. M-11/M-12는 자동화 스크립트가 실제 보증 범위보다 과장된 신호(fail-open PASS, 조용한 제외)를 내던 문제로, 검증 범위를 실제로 넓히거나 한계를 정직하게 문서화하는 방향으로 처리했다 |
| 결과 | Gate 6개, Checklist 5개, Skill 3개(security-review, planning, quality-gate), 스크립트 2개(validate-package, validate-references), 핵심 문서 4개(GATEGUARD, QUALITY_GATE, AGENT_WORKFLOW, QUICK_REF), ADR 3건의 오기가 수정됨. 두 번째 검수 리포트의 지적 사항(Critical 1 + Major 12 + Minor 6, 총 19건) 전부 처리 완료. |

---

## ADR-026. 두 번째 외부 검수 §12 사용자 확인 사항 5건에 대한 최종 결정

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 두 번째 외부 검수 리포트 §12가 사용자 확인이 필요한 사항 5건을 남김: (1) 환경 변경을 파괴적 범주와 분리할지, (2) Report 게시를 검증 후로 미룰지, (3) release-checklist가 제출용/운영배포용을 둘 다 맡을지, (4) 외부 근거 6종의 provenance를 장기 재검증할지, (5) SRS/API/DB Checklist 엄격도를 프로젝트 공통 최소선으로 올릴지 |
| 결정 | (1) **분리한다.** `04.GATEGUARD.md §6-1`에 "되돌릴 수 있는 로컬 환경 변경(비파괴적)" 행을 신설해 삭제·외부 설치(예외 없는 실시간 확인)와 분리하고, "사용자 승인 또는 유효한 근거"로 완화(구조 작업과 동일 대우). `00.QUICK_REF.md §2`도 동일하게 분리 — (2) 이미 ADR-024(C-01)에서 반영 완료, 추가 결정 불필요 — (3) 이미 ADR-025(M-10)에서 "적용 범위" 필드로 반영 완료, 추가 결정 불필요 — (4) **지금은 투자하지 않는다.** 외부 자료는 전부 대화 중 일회성 첨부이고 하네스가 아직 정식 저장소로 관리되지 않아, 안정 URL·해시를 추가해도 유지보수할 방법이 없다. `_SOURCE_MAPPING.md §0`의 "재현 필요 시 재첨부 요청" 한계 기록으로 충분하다고 판단. 실제 프로젝트 저장소에 하네스를 부착하는 시점에 재검토 — (5) **현재 방식(N/A + 근거) 유지, 획일적 최소선을 만들지 않는다.** 모든 프로젝트에 같은 엄격도를 강제하면 작은 프로젝트에서 형식적 N/A만 채우는 역효과가 날 수 있다고 판단 |
| 이유 | (1)은 실제로 필요한 위험 구분이었다(로컬 환경변수 수정과 삭제/외부설치가 지금까지 같은 급으로 묶여 있었음). (4), (5)는 지금 만들면 유지보수 부담만 늘고 실익이 적어 조건부로 미루되, "안 한다"가 아니라 "언제 재검토할지" 조건을 명시해 `_PENDING_IMPORT_LIST.md`류의 방치를 막는다 |
| 결과 | `04.GATEGUARD.md §6/§6-1`, `00.QUICK_REF.md §2`에 환경 변경 분리 반영. (4), (5)는 문서 변경 없이 이 ADR로 결정 근거만 남긴다. §12의 5건 전부 결론이 남 — 두 번째 외부 검수 리포트가 완전히 마무리됨. |

---

## ADR-027. 세 번째 외부 검수(2026-07-11) Critical 3건 + 사용자 결정 5건 반영, 결정 기록 위치 재정의

| 항목 | 내용 |
|---|---|
| 상태 | Accepted (단, `08.QUALITY_GATE.md §7-2`의 복합 Gate 매트릭스는 이후 라운드(ADR-028 등)에서 개인정보/파일업로드/관리자/일반코드 행이 추가되며 `Partially superseded` — 결제 관련 행 자체는 그대로 유효) |
| 배경 | 세 번째 외부 검수 리포트(SHA-256 대조, 독립 ZIP 구조 검사 포함)가 Critical 3건을 지적: (C-01) ZIP parity가 이름만 비교하고 내용은 안 봄, (C-02) Report 완료 트랜잭션이 게시 후 자기 자신을 다시 수정, (C-03) 복합 산출물(결제)에 Gate를 하나만 매핑. 사용자가 §12 관련 5가지 결정도 함께 확정함 |
| 결정 | C-01: `scripts/validate-package.mjs`에 SHA-256 내용 비교 추가(양성/음성 테스트 완료). C-02: `06.REPORT_TEMPLATE.md §8-2`를 재설계해 게시(zip 생성 포함) 이후에는 Report 파일을 더 수정하지 않고, 최종 검증 결과는 전달 메시지에만 기록. C-03: `08.QUALITY_GATE.md §7-2`(신설) 복합 Gate 누적·최악 판정 규칙, 결제·환불은 API+Security+Payment Gate 항상 동시 적용. 사용자 결정 5건: (1) 배포 SoT는 ZIP, (2) 최종검증은 Report가 아니라 채팅에 기록, (3) 결제 복합Gate 동의, (4) 외부근거 비보존 유지, (5) `05.WORKING_CONTEXT.md`를 신규 프로젝트 템플릿으로 전환(하네스 자체 이력은 `_WORKING_CONTEXT_HISTORY.md`로 전량 이관) |
| 이유 | C-01, C-02는 지난 라운드들에서 반복된 "자기 참조 갱신 누락"·"검증보다 게시가 먼저"라는 메타 패턴이 이번엔 더 깊은 층위(zip 내용 자체, Report 자기수정 순환)에서 재발한 사례다. C-03은 D-017("필수-판정 1:1")의 복합 산출물 버전이다. 05를 프로젝트 템플릿으로 바꾸면서 "앞으로 하네스 자체의 새 구조적 결정은 어디에 남기는가"라는 질문이 생겼는데, `10.ADR.md`가 이미 "의사결정 이력"을 소유하고 있으므로 앞으로도 그대로 사용하고, `_WORKING_CONTEXT_HISTORY.md`의 "GENERAL_HARNESS 자체 개발 이력 완결편" 섹션에 압축 결정 로그(D-023부터 이어서)를 계속 추가하기로 함 |
| 결과 | `scripts/validate-package.mjs`, `06.REPORT_TEMPLATE.md`, `08.QUALITY_GATE.md`, `skills/quality-gate/SKILL.md`, `skills/verification-loop/SKILL.md`, `05.WORKING_CONTEXT.md`, `_WORKING_CONTEXT_HISTORY.md` 수정. 앞으로 하네스 자체 결정은 ADR + `_WORKING_CONTEXT_HISTORY.md`의 D-로그로 계속 남긴다(05.WORKING_CONTEXT.md는 프로젝트 전용이라 더 이상 하네스 자체 결정을 기록하지 않는다). |

---

## ADR-028. 네 번째 외부 검수(재검수, .NET 독립 ZIP 검증 포함) Critical 5건 반영

| 항목 | 내용 |
|---|---|
| 상태 | Accepted (단, C-03의 "Report 상태 필드는 게시 후 갱신" 방식은 `Partially superseded by ADR-029` — 이후 필드 자체를 없애는 것으로 대체됨. 나머지 결정은 유효) |
| 배경 | 네 번째 외부 검수(재검수)가 독립적인 .NET ZIP 파서로 첨부 ZIP 자체(77개 파일, 해시 전부 일치)의 건전성은 확인했지만, 공식 절차 자체에 Critical 5건을 지적했다: (C-01) `validate-package.mjs`가 `unzip` 없는 환경(Windows)에서 검사를 못 해도 WARN+exit 0으로 게시를 허용, (C-02) 게시 트랜잭션에 백업·원자적 교체가 없어 실패 시 기존 정상 배포본을 잃을 위험, (C-03) 직전 라운드 Report 자신이 "게시 전"이라고 쓰여 있는데 `_LATEST.md`는 이미 "완료"를 가리킴, (C-04) 결제 복합 Gate 매트릭스가 `08.QUALITY_GATE.md`/`07.SECURITY_BASELINE.md`/스킬마다 다르게 표기(로그인까지 Payment Gate 적용하는 실제 버그 포함), (C-05) 대량 이동이 `09.AGENT_WORKFLOW.md`에서만 "파일 손상 가능"이 아니라 "구조 작업"으로 재분류되어 SoT와 충돌 |
| 결정 | C-01: `validate-package.mjs`를 `unzip` CLI 의존 없이 `node:zlib`만으로 ZIP Central Directory를 직접 파싱하도록 전면 재작성. 파싱 실패·ZIP 부재는 이제 FAIL(게시 차단). exact/case-insensitive 중복을 각각 검사(M-11 포함). C-02: `06.REPORT_TEMPLATE.md §8-2`에 임시 zip 생성→7종 검증→원자적 rename 교체 순서를 추가, 실패 시 기존 zip은 건드리지 않아 항상 안전. C-03: Report 상단 "상태" 필드(게시 전/완료)와 "최종 수치"(채팅 전달)를 분리해, 게시가 끝나면 상태 필드만 갱신. C-04: `08.QUALITY_GATE.md §7-2`에 산출물 유형별 단일 Gate 매트릭스를 확정하고 `07.SECURITY_BASELINE.md`·관련 스킬 4개가 전부 이를 참조하도록 통일. C-05: `09.AGENT_WORKFLOW.md §6`의 대량 이동 재분류를 삭제하고 SoT(`04.GATEGUARD.md §6-1`)와 통일 |
| 이유 | C-01, C-02, C-03은 전부 "검증·게시·자기수정" 트랜잭션 자체의 결함이며, 이 트랜잭션은 지난 세 라운드에 걸쳐 계속 다듬어 온 것이다 — 다듬을 때마다 새로운 층위의 결함이 발견됐다(1차: 순서 뒤바뀜, 2차: 자기수정 순환, 3차: 이번 라운드의 무결성·원자성·상태필드 분리). C-04는 "복합 산출물의 판정 매트릭스를 여러 문서에 각자 정의하면 반드시 어긋난다"는 D-017/D-024 원칙의 재확인이다 |
| 결과 | Critical 5건 + Major 14건 + Minor 8건, 총 27건 반영 완료. `validate-package.mjs`는 이제 어떤 OS에서도 `unzip` 설치 여부와 무관하게 동일하게 동작한다(4가지 시나리오 직접 테스트 완료). 새 원칙은 `_WORKING_CONTEXT_HISTORY.md`에 D-025, D-026으로 이어서 기록한다. `Partially superseded by ADR-029` — C-03의 "상태 필드는 게시 후 갱신" 방식은 이후 필드 자체를 없애는 것으로 대체됨. |

---

## ADR-029. 다섯 번째 외부 검수(2026-07-12) Critical 4건 반영

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 다섯 번째 외부 검수가 ZIP 자체(70개 Markdown, 전체 파일 78개, 해시 전부 일치, 7종 자동검증 PASS)의 건전성은 인정했지만 절차 문서에 Critical 4건을 지적했다: (C-01) `06.REPORT_TEMPLATE.md`가 `GENERAL_HARNESS.new.zip` 검증을 요구하는데 `validate-package.mjs`는 고정된 `GENERAL_HARNESS.zip`만 읽어 후보를 실제로 검증한 적이 없음, (C-02) ZIP 교체 후 패키지 내부 Report 상태를 다시 수정해 방금 만든 ZIP↔트리 SHA 일치를 즉시 깨뜨림, (C-03) 로그인/개인정보 등 복합 산출물의 Gate 라우팅이 `08 §7-2` SoT와 하위 skill Handoff에서 서로 다르게 축소됨, (C-04) 결제 Checklist의 위험 WARN이 "승인 또는 근거"(OR)로 완화되어 SoT의 AND 조건(승인+Payment Gate PASS+테스트 증거)을 우회 가능 |
| 결정 | C-01: `validate-package.mjs`에 `--zip <경로>` 인자를 추가해 실제 후보 경로를 명시 검사하고 `inspected_zip=`/`sha256=`을 출력하도록 함(양성/음성/기본경로 3가지 테스트 완료). C-02: Report 파일에는 애초에 "게시 상태" 필드를 두지 않기로 재설계 — 콘텐츠는 초안 작성 시 확정되고 이후 다시 수정하지 않으며, "게시 여부"는 `reports/_LATEST.md` 포인터와 archive 위치라는 외부 신호로만 판단한다. C-03: `08 §7-2` 매트릭스에 개인정보/파일업로드/관리자/일반코드 행을 추가하고, `security-review`/`verification-loop`/`quality-gate`의 Handoff에서 하드코딩된 부분 매핑을 제거해 전부 `quality-gate` 집계 경유로 단일화. C-04: 결제 Checklist 위험 WARN을 `04 §6-1`의 AND 조건(승인 AND Payment Gate PASS AND 테스트 증거)을 직접 인용하도록 교체 |
| 이유 | C-01/C-02는 "검증·게시" 트랜잭션이 라운드마다 새 층위의 결함을 드러낸 네 번째 사례다. 이번엔 "상태를 어떻게 안전하게 갱신할까"가 아니라 "애초에 그 상태를 이 파일 안에 둘 필요가 있는가"라는 더 근본적인 질문으로 해결했다 — 자기 참조 필드 자체를 없애는 것이 매번 안전한 갱신 순서를 다시 설계하는 것보다 근본적이다. C-03은 D-024(복합 Gate) 원칙이 매트릭스 확장 없이 하위 스킬에서 재요약되던 잔여 사례다. C-04는 D-017 원칙의 결제 특화 재발이다 |
| 결과 | Critical 4건 + Major 8건(M-01~M-08 전부) + Minor 7건, 총 19건 반영 완료(외부 검수 m-05로 6건→7건, 18→19 정정, 결정 자체는 그대로 유지). `10.ADR.md`에 "Partially superseded" 표기를 실제로 2건(ADR-025, ADR-027) 적용. 남은 §12 결정 사항(archive 16개 옛 경로 처리, GENERAL_HARNESS.zip을 프로젝트 인스턴스와 공유할지)은 사용자 확인 대기. |

---

## ADR-030. 배포 모델을 "템플릿 복제"로 확정 (§12 결정 2건 중 1건)

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | ADR-029의 §12 남은 결정 사항 중 "`GENERAL_HARNESS.zip`을 프로젝트 인스턴스와 공유할지"에 대해 사용자에게 "보통 하네스는 프로젝트별로 어떻게 쓰나"를 물어봄 |
| 결정 | **템플릿 복제 모델을 채택한다.** `GENERAL_HARNESS.zip`은 원본 템플릿 전용 이름으로 유지한다. 각 프로젝트는 이 템플릿을 각자 복제해서 그 프로젝트만의 사본으로 관리하며, 하네스 자체 개선사항은 이미 부착된 프로젝트 사본에 자동 반영되지 않는다(그때그때 수동 판단). 새 프로젝트는 항상 원본 템플릿에서 새로 복제한다. cookiecutter류 스캐폴딩 도구, `.cursorrules`/`AGENTS.md`류 AI 규칙 파일과 같은 배포 방식과 동일하다 |
| 이유 | 프로젝트마다 기술 스택·상태·결정사항이 다르므로(`05.WORKING_CONTEXT.md`를 프로젝트 전용 템플릿으로 이미 전환한 것과 같은 전제), 하나의 인스턴스를 여러 프로젝트가 실시간 공유하면 상태가 섞인다. "부착되는 순간 그 프로젝트만의 사본이 된다"는 원칙이 `05.WORKING_CONTEXT.md` 템플릿화(ADR-027) 결정과도 자연스럽게 맞아떨어진다 |
| 결과 | `09.AGENT_WORKFLOW.md §7-2`에 배포 모델을 명시하고, `06.REPORT_TEMPLATE.md §8-2`가 이를 참조하도록 정리했다. §12의 남은 결정 사항 중 1건(archive 16개 옛 경로 처리)만 미결로 남는다. |

---

## ADR-031. archive Report 원본 삭제 결정 (§12 마지막 결정, 제3의 선택지)

| 항목 | 내용 |
|---|---|
| 상태 | Accepted (단, "고아 행 검사를 삭제일 cutoff로 판단" 부분은 `Partially superseded by ADR-032` — 날짜 기반 추정이 같은 날 추가된 진짜 고아를 못 잡는 결함이 실제로 재현되어 명시적 파일 목록 방식으로 교체됨. 삭제 결정 자체와 삭제 로그 개념은 유효) |
| 배경 | §12에 남아있던 마지막 결정 사항은 원래 "archive Report 16개의 옛 literal 경로를 그대로 둘지, 경로만 기계적으로 보정할지"였다. 그런데 실제 대화에서는 "archive Report가 너무 많아졌다"는 이유로 **원본 자체를 삭제하고 `HISTORY_DIGEST.md` 요약만 남기자**는, 원래 제시했던 두 선택지와는 다른 제3의 결정이 나왔다. 삭제 전 "실제로 지우면 무엇이 달라지는지"를 직접 테스트해 사용자에게 먼저 알렸다(orphan 경고 발생, `validate-references.mjs` FAIL 발생을 실측으로 확인) |
| 결정 | archive Report 30개(2026-07-12 기준 전부)의 원본을 삭제하고 `reports/archive/HISTORY_DIGEST.md`만 유지한다. `HISTORY_DIGEST.md`에 "삭제 로그" 표를 신설해 삭제일·대상·사유·근거를 기록한다. `06.REPORT_TEMPLATE.md`의 "오래된 Report는 삭제하지 않는다"는 기본 원칙에, "사용자가 명시적으로 요청하면 요약 선행 보존 + 삭제 로그 기록 조건으로 삭제할 수 있다"는 예외 경로를 추가한다. `scripts/validate-report-consistency.mjs`의 고아 행 검사를 삭제 로그의 cutoff 날짜 인지형으로 개선해, 삭제 로그 이전 항목은 고아여도 경고하지 않고 그 이후 항목만 검사한다(이 방식은 이후 ADR-032에서 결함이 발견되어 교체됨) |
| 이유 | 파일 삭제는 `04.GATEGUARD.md §6-1`의 "파일 손상 가능 작업"(예외 없이 항상 실시간 사용자 확인 필요)에 해당한다. 이번 삭제는 사용자가 명확히 "지우려고"라고 확인한 뒤 진행했다. 다만 "삭제하지 않는다"는 지금까지의 기본 원칙을 무조건 지키기보다, 실제로 사용자가 요청하면 안전장치(요약 선행 보존, 삭제 로그, 검증 스크립트의 cutoff 인지)를 갖춰 예외를 허용하는 것이 더 실용적이라고 판단했다 |
| 결과 | `reports/archive/`에는 이제 `HISTORY_DIGEST.md`만 남는다(30개 원본 삭제). 이 삭제로 깨졌던 문서 내부 참조 3건(`_WORKING_CONTEXT_HISTORY.md` 1건, `_SOURCE_MAPPING.md` 2건)을 함께 수정했다. §12의 5개 결정 사항이 전부 마무리됐다. |

---

## ADR-032. 고아 행 검사의 날짜 cutoff 방식을 명시적 파일 목록 방식으로 교체

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | ADR-031에서 만든 "삭제 로그 날짜 이후 항목만 고아 검사" 로직에, `HISTORY_DIGEST.md`에 삭제일(2026-07-12)과 **같은 날짜**로 실제 존재하지 않는 파일(`2026-07-12_same-day-missing-probe_report.md`)을 가리키는 행을 추가해 검증한 결과, `validate-report-consistency.mjs`가 이를 못 잡고 PASS를 반환하는 것이 실제로 재현됨. 원인은 `dateMatch[1] > purgeCutoff`처럼 **엄격한 초과 비교**를 써서, cutoff와 같은 날짜는 "cutoff 이전"으로 취급돼 검사 대상에서 빠졌기 때문 |
| 결정 | 날짜 기반 추정을 완전히 폐기하고, `HISTORY_DIGEST.md`의 `<!-- PURGED_FILES_START -->` ~ `<!-- PURGED_FILES_END -->` 사이에 **실제로 삭제된 30개 파일명을 전부 명시적으로 나열**한다. `scripts/validate-report-consistency.mjs`는 이 목록에 있는 파일명만 "삭제 확인됨"으로 인정하고, 목록에 없는 고아 행은 날짜와 무관하게 전부 경고한다. 정상 상태(PASS)와 오염된 상태(동일한 probe 행 추가 시 WARN 발생) 둘 다 재현·확인했다 |
| 이유 | 날짜 같은 하루 단위 정보로는 "이 항목이 그때 삭제된 30개 중 하나인지"와 "이 항목이 그날 이후 새로 생긴 진짜 문제인지"를 구분할 수 없다. 명시적 목록은 이 모호함 자체를 없앤다 — 파일명이 목록에 있으면 삭제가 맞고, 없으면 무조건 의심 대상이다 |
| 결과 | 같은 문제가 세 번째로 반복되는 것을 막았다("자기 참조 문서/검사 로직의 갱신 누락·모호한 추정" 계열 — `04.GATEGUARD.md §11-3`의 기존 패턴과 같은 계열). 이 삭제 로그 방식은 이후 새로운 삭제가 생길 때마다 파일명을 목록에 그대로 추가하는 식으로 확장한다. `Partially superseded by ADR-033` — 명시적 목록 자체의 구조적 무결성(marker 유일성, 목록⊆digest, 목록∩archive=∅, count 일치, 연도 일반화)은 이후 강화됨. |

---

## ADR-033. 여섯 번째 외부 검수(재검수) Major 10건 반영 — 게시 트랜잭션·Gate 라우팅·검증 로직 정합화

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 여섯 번째 외부 검수(재검수, 첨부 ZIP 52개 파일 독립 대조, Critical 0건)가 Major 10건을 지적했다: (M-01) Report 트랜잭션이 validator FAIL만 롤백, I/O 실패는 미대응, (M-02) project Report 완료 경로가 §8-2와 직접 충돌, (M-03) 복합 Gate 흐름도가 여전히 단일 분기, (M-04) API/DB/Payment Gate 필수-판정 1:1 재발, (M-05) Verification·Gate·Checklist 전역 최종판정 공식 없음, (M-06) 지난 라운드에 만든 PURGED_FILES 명시 목록도 연도 하드코딩·부분문자열 검사·자기승인 우회가 가능, (M-07) 구조 WARN이 게시를 막지 못함, (M-08) archive 삭제 예외가 다른 문서에 전파 안 됨, (M-09) Source Mapping의 4차 이후 provenance 누락과 5차 wildcard, (M-10) 직전 Report의 43/44 수치 차이와 "다음 작업 없음" 오류. M-06은 실제로 3가지 공격 시나리오(2027년 고아, 무단 목록 추가, 개수 조작)로 재현 테스트해 확인함 |
| 결정 | M-01: 트랜잭션 9번 단계로 I/O 실패도 validator FAIL과 동일 롤백. M-02: `06.REPORT_TEMPLATE.md §8-3`(프로젝트 Report 전용, ZIP 없음) 신설. M-03: 흐름도를 fan-out 구조로 재설계. M-04: API/DB/Payment Gate PASS/FAIL을 필수 항목과 완전히 재정렬. M-05: 전역 최종판정 공식(`maxSeverity(Verification, Gate 전부, Checklist 전부)`) 신설, 5개 Checklist "결과 요약"을 로컬 판정으로 재정의. M-06: `validate-report-consistency.mjs`를 digest 표 구조적 파싱 + marker 유일성 + 집합 연산(purged⊆digest, purged∩archive=∅) + count 일치 + 연도 일반화로 전면 재작성. M-07: 트랜잭션에 WARN 분류·해제 단계 추가. M-08: 삭제 예외를 4개 문서에 전파. M-09: Source Mapping에 4~6차 provenance 보강. M-10: 원본은 불변, 이 Report에 정정 기록만 남김 |
| 이유 | M-06은 지난 라운드(ADR-032)에 만든 "명시적 목록" 방식도 완벽하지 않다는 것을 보여준다 — 목록 자체의 구조적 무결성(형식, 중복, 다른 집합과의 관계, 개수 일치)까지 검증해야 진짜 안전하다. M-01~M-05, M-07은 "검증에 성공해도 게시를 막지 못하거나(WARN), 트랜잭션이 일부 실패 유형만 다루거나(I/O), 여러 판정 소스가 있는데 통합 규칙이 없는" 계열의 문제로, 전부 "부분적으로만 정의된 안전장치는 결국 우회된다"는 하나의 교훈으로 수렴한다 |
| 결과 | Major 10건 + Minor 5건, 총 15건 반영 완료. `scripts/validate-report-consistency.mjs`의 새 로직을 4가지 시나리오(정상/2027고아/무단추가/개수불일치)로 재현 테스트. 이 과정에서 마커 카운트 정규식 자체의 버그(설명 문구 속 마커 이름 언급까지 세어 정상 상태도 FAIL 오판)를 발견해 즉시 수정. `Partially superseded by ADR-034` — M-06의 "명시적 단일 목록" 방식은 이후 event 기반 구조로 대체됨(승인 자기결속·다중 삭제 지원). |

---

## ADR-034. 일곱 번째 외부 검수(재검수) Major 9건 반영 — 삭제 이벤트 모델, 나머지 잔여 정합화

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 일곱 번째 외부 검수(재검수, Critical 0건, 첨부 ZIP 53개 파일 독립 대조)가 Major 9건을 지적했다. 가장 심각한 것은 M-06/X-06 — 지난 라운드(ADR-033)의 "명시적 목록" 방식도 "digest 행+목록+누계 count를 함께 조정"하면 새 승인 없이 조용히 통과하고(자기승인 우회를 실제로 재현해 확인), 반대로 정상적인 두 번째 삭제 이벤트는 오히려 실패시켰다(단일 누계 count 하나뿐이라 새 이벤트를 더하면 옛 count와 안 맞음). 그 외 M-01(초안 롤백 누락), M-02(§8-3 참조 잔존 오류, 첫 프로젝트 전환 미정의), M-03(외부연동 Security 우회, 계약Gate에 Verification 강제 없음), M-04(DB Index 필수·PASS·WARN 중첩), M-05(Release 비밀값 분리, Payment 로그의 §2/결과요약 불일치), M-07(06 §8-1 자체가 "이동만" 단정), M-08(SourceMapping 5·6차 통계가 서로 뒤바뀜), M-09(직전 Report의 44/45 오차, 이전 정정에서도 반복됨) |
| 결정 | M-06: 삭제를 "이벤트"로 구조화 — `HISTORY_DIGEST.md`에 `PURGE_EVENT`(event_id/date/approval/count를 헤더에 갖는 독립 블록)를 도입하고 기존 이벤트는 절대 고치지 않고 새 이벤트만 추가한다. `scripts/validate-report-consistency.mjs`가 각 이벤트를 독립 검증(event_id 유일성, count-목록 일치, approval이 실제 ADR인지, 목록이 digest 부분집합인지, archive와 안 겹치는지)한다. 5가지 시나리오(정상/정상 2차삭제/자기승인 시도/가짜ADR인용/중복ID)로 재현 테스트 완료. M-01: 미게시 초안도 롤백·격리 대상으로 명시. M-02: 188행 참조를 §8-3으로 교체, 첫 프로젝트 전환 시 하네스 Report는 그대로 두는 분기 추가. M-03: 비민감 경로도 외부연동 확인을 거치도록 흐름도 수정, 계약 Gate 전체에 "실제 코드 있으면 verification-loop 항상 누적" 규칙 추가. M-04: DB Index를 필수 항목 표에서 분리해 별도 "권장 항목" 절로 이동. M-05: Release §2 FAIL 목록에 비밀값 분리 반영, Payment Checklist PASS에 로그 기록 명시. M-07: §8-1의 "이동만" 단정을 retained/purged 두 상태로 재서술, 05.WORKING_CONTEXT §4에도 반영. M-08: Source Mapping의 5·6차 통계 뒤바뀜 정정. M-09: 이 Report 자체 |
| 이유 | M-06은 "명시적 목록도 완벽하지 않다"를 두 번째로 보여준 사례다 — 이번엔 "목록 내용을 검증"하는 것만으로는 부족하고 "그 목록이 시간에 따라 어떻게 변해야 하는지"(불변 이벤트 누적)까지 구조로 강제해야 한다는 교훈을 얻었다. 이건 근본적으로 "파일 기반 시스템은 쓰기 권한이 있는 누구나 과거 기록을 고칠 수 있다"는 한계를 완전히 없애지 못하지만, 최소한 "그냥 숫자 하나 바꾸기"보다 "새 불변 이벤트를 명시적으로 추가하기"가 훨씬 눈에 띄는 행위가 되도록 만들었다 |
| 결과 | Major 9건 전부 반영. 삭제 이벤트 모델을 5가지 시나리오로 직접 테스트(정상/정상 2차삭제/자기승인 시도/가짜ADR/중복ID 전부 기대대로 동작). Minor 5건도 함께 반영(검증기 개수 하드코딩 잔여, Quality Gate 노드명/참조 오류, Payment 로그 로컬 경계, Checklist 필드명, report-consistency PASS 문구 정확화). |

---

## ADR-035. 게시 트랜잭션에 "실제 전달" 단계 명시 — 자체 검수 7회차(반복루프 4회차)

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | Claude가 4회차 자체 검수(용어 전수 정리)를 마치고 검증·원자적 교체까지 전부 정상 수행했는데, 그 산출물을 실제 사용자에게 전달하는 것 자체를 깜빡했다. 사용자가 "왜 수정한 하네스를 안 보내주냐"고 물어서야 발견됨. `06.REPORT_TEMPLATE.md §8-2`를 확인한 결과, 이 절차가 "임시 zip 생성 → 검증 → 원자적 교체"까지만 규정하고 "그 산출물을 실제로 사용자에게 건네는 것"은 어디에도 명시돼 있지 않았다(`09.AGENT_WORKFLOW.md`, `00.HARNESS_RULES.md`, `04.GATEGUARD.md` 전체 grep으로도 확인) |
| 결정 | `06.REPORT_TEMPLATE.md §8-2`의 7번(원자적 교체) 뒤에 7-1번을 신설해 "산출물을 출력 위치로 복사 → 실제 전달 조치 → 전달한 파일이 검증한 후보와 동일한지 재확인 → 최종 결과를 전달 메시지에 기록"을 게시 절차의 정식 마지막 단계로 명시한다 |
| 이유 | 이 문서 세트가 그동안 정성 들여 만들어온 안전장치(검증 후 게시, 원자적 교체, WARN 해제 확인 등)는 전부 "패키지 파일 자체의 무결성"을 지키는 데는 완벽했지만, "그 결과를 실제로 사용자 손에 쥐어주는 마지막 한 걸음"은 암묵적 기대로만 남아 있었다. 그리고 그 암묵적 기대가 실제로 깨졌다 — 추론이 아니라 실제로 일어난 실패다 |
| 결과 | `06.REPORT_TEMPLATE.md §8-2`에 7-1번이 추가됨. 앞으로 게시 트랜잭션은 "패키지가 안전하게 만들어졌는가"뿐 아니라 "그 패키지가 실제로 사용자에게 도착했는가"까지 확인해야 완료로 본다. |

---

## ADR-036. `ui-ux-design` 스킬 신규 추가 — 스킬 본체와 기술 스택 정책의 분리 확정

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 화면 설계(UI/UX) 작업을 정식 스킬로 등록해달라는 외부 검수 의견을 받았다. 검수 의견에는 두 가지 성격이 섞여 있었다 — (a) 스킬 자체를 어떻게 안전하게 구조화할지에 대한 제안(수용)과 (b) 검수 대상 ZIP 자체가 배포 오류 상태라는 전제(실제로는 검수자가 `CLAUDE.md`와 `GENERAL_HARNESS/`를 직접 합쳐 만든 별도 테스트용 zip이었고, 이 하네스가 실제로 배포한 zip과는 무관함이 SHA-256 대조로 확인됨). 후자는 거절하고 전자만 반영했다 |
| 결정 | `skills/ui-ux-design/SKILL.md`(기술 중립적 실행 절차)와 `skills/ui-ux-design/STACK_PROFILE_PRIMEVUE_TAILWIND.md`(PrimeVue+Tailwind 고정 정책, 프로젝트가 선택적으로 활성화)를 분리해 신설했다. `gates/ui-ux-gate.md`, `checklists/ui-ux-checklist.md`도 함께 신설했다. `08.QUALITY_GATE.md §7-2` 매트릭스에 화면 설계 관련 행(일반/로그인/관리자/개인정보/결제 화면)을 추가했다. `02.SKILL_INDEX.md`에 13번째 Active 스킬로 등록했다 |
| 이유 | 이 하네스는 이미 "프로젝트 전용 규칙을 전역 스킬로 고정하지 않는다"는 원칙(`_PENDING_IMPORT_LIST.md`의 `frontend-patterns` 제외 사유, `skill-scout`의 Anti-Pattern)을 갖고 있었다. PrimeVue/Tailwind처럼 이번 프로젝트에만 해당하는 기술 선택을 스킬 본체에 하드코딩하면 이 원칙과 정면으로 부딪힌다. Stack Profile을 별도 파일로 분리해 "스킬은 절차를 소유하고, 기술 정책은 선택적으로 활성화되는 별도 문서가 소유한다"는 구조로 풀었다 |
| 결과 | 스킬 13개, Gate 7개, Checklist 6개로 갱신(`05.WORKING_CONTEXT.md`, `HISTORY_DIGEST.md` 반영). "검수 대상 ZIP이 실제 배포본과 다르다"는 사실은 해시 대조로 직접 확인 후 근거 문서에 기록했고, 그에 기반한 P0 항목(단일 루트 복구, Adapter 신설 등)은 반영하지 않았다. |

---

## ADR-037. `ui-ux-design`에 기계적·획일적 결과 방지 검토 추가

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 기존 `ui-ux-design`은 사용자 목표, 흐름, 상태, 반응형, 접근성, 구현 가능성을 충분히 다뤘지만, 결과물이 기계적으로 반복되거나 시각적 계층·문구·토큰 일관성이 약한 경우를 별도로 검토하지 않았다. 사용자는 이를 "AI 티"로 인식되는 결과를 줄이는 방향으로 보강해 달라고 요청했다. |
| 결정 | 기존 `ui-ux-design` 스킬, UI/UX Gate, UI/UX Checklist에 기술 중립적인 "기계적·획일적 결과 방지 검토"를 추가한다. 검토 범위는 토큰과 표면 규칙, 시각적 계층, 반복과 리듬, 장식 절제, 콘텐츠 현실성, 선택 근거다. 특정 미감·비대칭·기술 스택을 강제하지 않으며, 검토 또는 근거가 부족하면 일반 WARN으로 드러낸다. 핵심 행동을 식별할 수 없게 만드는 시각적 계층 문제는 FAIL이다. |
| 제외 | 특정 UI 라이브러리나 CSS 프레임워크 규칙을 공통 스킬에 추가하지 않고, 자동화 스크립트로 주관적 시각 품질을 판정하지 않는다. 프로젝트별 Stack Profile의 구현 규칙은 공통 스킬과 분리해 유지한다. |
| 이유 | 화면의 의도적 반복·비대칭·강한 표현은 서비스 목적에 따라 유효할 수 있으므로 금지 목록으로 처리하면 안 된다. 대신 사람이 근거를 남기며 검토하도록 해야 디자인 품질을 높이면서도 기술 중립성과 프로젝트별 표현의 자유를 유지할 수 있다. |
| 결과 | 화면 설계 산출물은 기존 UX·접근성·구현 가능성 기준에 더해, 결과물의 획일성·장식 과다·문구 현실성을 명시적으로 검토한다. Stack Profile 분리 원칙(ADR-036)은 그대로 유지한다. |

---

## ADR-038. PrimeVue + Tailwind Stack Profile의 구현 경계와 예외 기록 보강

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | ADR-037의 공통 검토 기준은 기술 중립적으로 유지해야 한다. 다만 PrimeVue + Tailwind CSS를 실제로 쓰는 프로젝트에서는 디자인 토큰을 어디에 연결하고, 표준 컴포넌트 대신 커스텀 구현을 언제 허용할지에 대한 구현 경계가 더 필요하다. |
| 결정 | `STACK_PROFILE_PRIMEVUE_TAILWIND.md`에 토큰·테마 연결, 커스텀 컴포넌트와 스타일 예외의 허용 조건·기록 항목, 구현 전·후 확인 항목을 추가한다. PrimeVue는 동작·상태·접근성이 필요한 표준 컴포넌트를, Tailwind는 레이아웃과 의미 기반 토큰 적용을 담당한다. |
| 이유 | 공통 스킬에 특정 라이브러리 규칙을 넣지 않으면서도, 실제 구현에서 토큰 이탈·표준 컴포넌트 중복·임의값·내부 덮어쓰기의 누적을 막을 수 있다. 예외를 금지하지 않고 근거·상태·재사용 범위를 기록하면 프로젝트 고유 UI도 안전하게 허용할 수 있다. |
| 결과 | PrimeVue + Tailwind를 활성화한 프로젝트는 공통 UI/UX 검토를 구현 단계까지 일관되게 이어갈 수 있다. 다른 스택에는 적용되지 않으며, Profile은 선택적으로만 활성화된다. |

---

## ADR-039. Git-first 공식 반영과 `AGENTS.md` 단일 진입점

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | ZIP을 배포 Source of Truth로 두던 규칙은 Git 저장소 중심 운영과 충돌했고, 기존 단수형 루트 에이전트 문서는 표준 자동 인식 파일명과 일치하지 않았다. |
| 결정 | Git 저장소를 배포·이력 Source of Truth로 한다. 작업 브랜치 push는 공유 진행 상태, `main`에 병합된 커밋만 공식 반영이다. 루트 에이전트 진입점은 `AGENTS.md` 하나로 표준화하고 기존 단수형 문서는 제거한다. `CLAUDE.md`는 보조 안내로 유지한다. 두 진입 문서는 자동 검증 범위에서 제외하며 변경 시 수동 검수한다. |
| 이유 | 작업 중인 변경과 공식 반영을 구분하면서, 도구 호환성이 높은 단일 진입점과 과도하지 않은 운영 기록을 유지한다. |
| 결과 | Report 완료 절차는 Git 기준으로 정리되고, ZIP 검증은 사용자가 export를 요청한 경우에만 선택적으로 수행한다. 과거 ZIP 관련 ADR·Report는 당시 결정의 역사 기록으로 보존한다. |

---

## ADR-040. `intent-to-implementation` 축소 반입과 보조 참조 문서 규칙 공식화

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 외부 제공 스킬 패키지 `intent-to-implementation`은 "요청 접수→조사→요구사항 재구성→계약→계획→구현→검증→설명" 전 파이프라인을 자체 소유하는 독립형 미니 하네스였다. 그대로 반입하면 같은 단계 규칙이 두 곳에 생기고 `00.HARNESS_RULES.md §3` 바깥에 평행 우선순위가 생긴다(반입 검수 P-16). 또한 영어 본문, YAML frontmatter, 벤더 배포 파일(`agents/openai.yaml`, icon), 마크다운 링크 참조 등 하네스 표준과 어긋나는 형식 문제가 있었다(P-15, P-18). 한편 실사용 Report 10건 분석에서 이 스킬의 requirement-reconstruction이 잡았어야 할 실패(services→domain 같은 날 재작업, P-05)가 실제로 발생해 반입 근거는 실증됐다. |
| 결정 | (1) 기존 문서에 소유자가 없는 4개 가치 — 의도 라우팅, 요구사항 재구성(Confirmed/Inferred/Unresolved), 최소 해법 나침반, 변경 설명 형식 — 만 유지하고, 소유자가 있는 절차(작업 계약=`AGENTS.md`, 사전 조사=`CLAUDE.md §1`, 계획=`planning`, 검증=`verification-loop`)는 참조로 치환해 14번째 스킬로 반입한다. (2) 본문은 한국어 하네스 7섹션 표준으로 재작성하고 frontmatter·벤더 파일은 반입하지 않는다. (3) 참조 4종은 `references/` 하위 폴더 대신 스킬 폴더 바로 아래 `REFERENCE_*.md` 평면 구조로 두고, 이 구조를 `01.SKILL_TEMPLATE.md §2-1` "보조 참조 문서 규칙"으로 공식화한다(선례: `ui-ux-design`의 STACK_PROFILE 분리, ADR-036). |
| 이유 | 스킬은 하네스의 하위 모듈이라는 원칙(`00.HARNESS_RULES.md §2-3`)을 지키려면 파이프라인 소유권이 아니라 공백 4개만 가져와야 한다. 참조 문서를 평면 구조로 두면 새 폴더 규칙 없이 기존 선례와 `validate-references.mjs` 코드 span 검사를 그대로 활용할 수 있다. |
| 결과 | 활성 스킬 14개. 사용률 0% 스킬이 9개인 상태에서의 추가라는 우려(P-04)는 skill-stocktake 판정을 같은 작업에서 선행하는 것으로 처리했다 — 이 스킬은 발동 조건이 "모든 모호한 요청"이라 기존 0회 스킬들과 발동 빈도 특성이 다르다. 원저작 출처는 미확인 상태로 `_SOURCE_MAPPING.md §0`에 기록한다. |

---

## ADR-041. `_LATEST.md` 2행 구조 — 하네스 최신과 프로젝트 최신의 분리

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 하네스는 프로젝트마다 부착하는 방식으로 운영되는데 `reports/_LATEST.md`는 전역 포인터 1개만 지원했다. 그 결과 부착 프로젝트(챱챱)의 Report 10건이 하네스 저장소로 환류되지 않았고, 프로젝트 Report와 하네스 Report가 같은 `reports/`에 섞이면 "하네스 최신"이 superseded 누락 WARN으로 오판되는 문제가 실제 발생했다(실사용 검수 P-07). |
| 결정 | `_LATEST.md`를 "하네스 유지보수 최신 / 부착 프로젝트 최신" 2행 구조로 확장한다. 두 행은 서로 다른 이력이므로 한쪽 갱신이 다른 쪽을 superseded로 만들지 않는다(`06.REPORT_TEMPLATE.md §8-2` 4-b의 "서로 다른 이력은 이동하지 않는다"와 일치). 부착 프로젝트에서 하네스 규칙의 공백·결함이 드러난 경우, 그 프로젝트 Report의 요약을 하네스 저장소의 다음 유지보수 Report에 근거로 인용해 환류한다. |
| 이유 | 폴더 분리(`reports/project/`)안보다 변경 범위가 작고, 부착형 운영에서 두 이력의 최신 포인터를 각각 유지할 수 있는 최소 구조다. |
| 결과 | 하네스 저장소가 자신의 실사용 데이터를 받을 공식 경로가 생긴다. 이번 작업 자체가 첫 환류 사례다(챱챱 Report 10건 분석 → 하네스 수정). |

---

## ADR-042. `browser-qa` D등급 장기 보류 → 정식 스킬 승격

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 챱챱 실사용 Report 10건 중 5건이 존재하지 않는 스킬 "Browser"를 적용했다고 기록했다(실사용 검수 P-03). 검증 항목에는 DOM 치수·색상 대비·터치 영역의 실측 수치가 남아 있어 실제 브라우저 측정 절차가 반복되고 있었고, 사용자 확인 결과 자동화 도구를 사용한 측정이었다. `_PENDING_IMPORT_LIST.md §2`는 `browser-qa`를 "UI 회귀 테스트가 반복될 때" 도입 조건의 D등급으로 보류 중이었는데, 조건이 이미 충족된 상태였다(스킬 추가 기준 반복성 3회 이상도 충족). |
| 결정 | `skills/browser-qa/SKILL.md`를 15번째 정식 스킬(코드 스킬)로 신설한다. 핵심 요구는 재현 가능성 — 도구명·뷰포트 px·측정 수단·측정값을 함께 기록해 다음 세션이 같은 검증을 재현할 수 있어야 PASS다. 미등록 명칭("Browser")으로 스킬 적용을 기록하는 것은 FAIL로 명시한다. 특정 자동화 도구를 하네스 전역 기본값으로 고정하지 않는다. |
| 이유 | 절차는 이미 실사용에서 5회 검증됐고, 문서화되지 않아 "어떤 명령으로 확인했는지"가 재현 불가능하게 남는 부작용만 있었다. 스킬로 승격하면 실측 관행을 그대로 유지하면서 기록 재현성만 강제할 수 있다. |
| 결과 | 활성 스킬 15개. `_PENDING_IMPORT_LIST.md §2`의 해당 행은 승격 완료로 갱신. 과거 Report의 "Browser" 표기 정정 주석은 챱챱 폴더 측 작업으로 남는다. |

---

## ADR-043. 스택 한정 스킬 범주 신설과 `vue-ui-polish` 반입

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | `_PENDING_IMPORT_LIST.md §3`은 "특정 프론트엔드 스택 기준을 전역 규칙으로 오해할 수 있음"을 이유로 `frontend-patterns` 원본 전체를 X등급 제외했다. `vue-ui-polish`는 이름부터 Vue 3 고정이라 같은 사유에 걸린다(반입 검수 P-17). 기존 해법(ADR-036)은 스킬 본체를 기술 중립으로 두고 스택 정책만 Stack Profile로 분리하는 것이었지만, 이 스킬은 절차 전체가 Vue에 결합되어 분리할 기술 중립 본체가 남지 않는다. 한편 이 하네스는 프로젝트마다 따로 부착해 쓰고 프로젝트 간에 이관하지 않는 운영 방식이라(사용자 확인, 2026-08-03), 스택 한정 스킬이 다른 스택 프로젝트를 오염시킬 경로가 구조적으로 약하다. |
| 결정 | "스택 한정 스킬" 범주를 신설한다(`01.SKILL_TEMPLATE.md §2-2`). 조건: Harness Control Rule에 발동 스택을 명시하고, 해당 스택이 아닌 프로젝트에서는 스킬의 존재 자체를 무시한다고 적는다. 이 조건을 충족하는 `vue-ui-polish`를 16번째 스킬로 반입한다. MIT 파생 작업(Emil Kowalski, Skills for Design Engineers)이므로 `skills/vue-ui-polish/LICENSE`를 함께 반입하고 SKILL.md의 파생 고지 문장을 유지한다(P-19) — 하네스에 외부 LICENSE 파일이 들어오는 첫 사례다. 벤더 배포 파일(`agents/openai.yaml`, icon)과 frontmatter는 기존 결정(ADR-040)과 동일하게 제외한다. |
| 이유 | X등급 제외 사유의 본질은 "스택 기준의 전역 오해"이지 스택 기준 자체가 아니다. 발동 경계를 스킬이 스스로 선언하면 오해 경로가 차단되고, 실사용 근거(챱챱 Report 10건 중 8건이 UI/UX 작업, 터치 영역·색상 대비·reduced-motion 문제가 반복 등장)가 이 스킬의 references와 정확히 겹친다 — 실증 근거가 가장 강한 반입이다. |
| 결과 | 활성 스킬 16개. `_PENDING_IMPORT_LIST.md §3`의 `frontend-patterns` 행에 스택 한정 스킬 예외를 추기한다. 다른 스택 전용 스킬(React 등)도 같은 조건을 충족하면 이 범주로 반입할 수 있다. |

---

## ADR-044. 집행층 도입 — 규칙의 문서 의존을 기계 강제로 전환

| 항목 | 내용 |
|---|---|
| 상태 | Accepted |
| 배경 | 두 종류의 실증 근거가 쌓였다. (1) 실사용 Report 10건 분석에서 "규칙은 있는데 실행되지 않는" 불이행이 반복 확인됐다 — planning 발동 조건 충족 7건 중 0회, git-workflow 0회, Git 이력 기록 10/10 누락. 산문 규칙은 읽혀야만 작동하는데, 문서가 71개로 늘어나며 읽기 자체가 생략됐다. (2) 2026-08-02~03에 실제 유출 사고 2건 — 민감 검수 문서·외부 zip이 PUBLIC 저장소에 push되어 이력 재작성으로 제거했고, 로컬 에이전트 설정(`.claude/settings.json`)이 `git add -A`에 딸려 커밋됐다. 사람/에이전트의 주의에 의존하는 통제는 실측 준수율이 낮다. |
| 결정 | 규칙을 4가지 기계 장치로 변환한다. (1) **CI**: `.github/workflows/harness-ci.yml`이 push(main)/PR마다 검증 스크립트 7종을 강제 실행한다. (2) **시크릿 검사**: `scripts/validate-no-secrets.mjs` 신설 — 금지 파일 패턴(`.claude/`, zip, .env, 키 파일)과 알려진 토큰 패턴을 Git 추적 파일에서 검사하고 CI에 포함한다. (3) **브랜치 보호**: `main`에 required status check(validate)·force-push 금지·삭제 금지를 설정해 "main 병합만 공식 반영" 규칙을 저장소 설정으로 강제한다. (4) **PR 템플릿**: Report·GateGuard·검증 확인 항목을 `.github/pull_request_template.md`로 두어 PR마다 눈에 보이게 한다. 추가로 **2층 읽기 모델**(`03.CONTEXT_BUDGET.md §2-2-1`)을 도입해 상시 표면을 4개+α 문서로 압축하고, 나머지는 트리거 기반 로딩으로 강등한다. |
| 이유 | 이 하네스의 규칙층은 실증 사고 기반이라 걷어낼 수 없지만, 준수를 문서 읽기에 의존하는 한 같은 불이행이 반복된다. 현업 표준(CI·branch protection·PR 템플릿)은 잊어도 작동하는 장치이고, 기존 검증 스크립트가 순수 Node라 CI 전환 비용이 거의 없다. 읽기 표면 압축은 "문서가 많을수록 준수율이 떨어진다"는 실측 역학에 대한 직접 대응이다. |
| 결과 | 검증 스크립트 8종(export 전용 validate-package 포함), CI 상시 7종. 하네스 수정이 main에 들어가려면 사람이 기억하지 않아도 검증을 통과해야 한다. 문서 규칙과 기계 장치의 역할 분담: 문서는 판단 기준을 소유하고, CI/보호 설정은 이행을 강제한다. |

